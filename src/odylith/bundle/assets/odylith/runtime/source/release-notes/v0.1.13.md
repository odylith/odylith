---
version: 0.1.13
title: Cross-Host Greenfield Intelligence and Runtime Integrity
published_at:
note_link_label: Open release note on GitHub
external_link_label: GitHub release
reopen_label: v0.1.13 update
summary: v0.1.13 hardens consumer-lane startup, context, intervention, migration, and greenfield proposal flows while keeping host-reasoned intelligence, provider-free Odylith validation, and source-truth boundaries intact.
highlights:
  - Empty and thin consumer repos now get confirmation-gated greenfield proposals with backlog, program waves, planned Registry components, host-authored Atlas topology, release plan, assumptions, risks, and validation.
  - Domain Intelligence now uses an open-world host-reasoning contract instead of a fixed in-code taxonomy; Odylith validates accepted proposals, evidence tiers, topology hygiene, and apply safety before writes.
  - Cross-host startup, context, hooks, memory, intervention, governance records, and release proof stay active while always-loaded guidance, repeated GA proof, and noisy timeline capture are reduced.
---

# Cross-Host Greenfield Intelligence and Runtime Integrity

Odylith v0.1.13 focuses on the consumer-lane experience: the runtime should be
fast, accurate, and helpful even before a repo has application source. A vague
request such as "build an ecommerce site" no longer dead-ends at missing source
evidence. Odylith can now draft a proposal first, label source posture and
assumptions explicitly, and wait for confirmation before writing governed truth.

The greenfield path is host-agnostic and provider-free inside Odylith by
default. Claude, Codex, or another host uses its normal reasoning capability to
author the open-world project proposal from the prompt and shallow repo
evidence. Odylith supplies the strict contract around that reasoning: source
posture, evidence tiers, required sections, topology hygiene, confirmation, and
owned apply paths. That keeps the system useful for arbitrary product, science,
math, research, data, infrastructure, education, art, policy, and mixed-project
requests without pretending a small checked-in taxonomy can cover the world.

This release also tightens the runtime integrity work behind that UX. Startup and
Context Engine order are kept serial and explicit, hooks stay quieter on success,
Compass can record accepted proposal memory, Casebook labels stay compact, Radar
topology is treated as a first-class spine, release proof avoids duplicate
full-suite reruns at GA, and migration gates now cover the 0.1.13
consumer-surface changes.

- `odylith show` in an empty repo now points to greenfield proposal mode instead
  of treating missing source as a dead end.
- `odylith greenfield propose` returns a host-reasoning contract with repo
  posture, required proposal sections, evidence-tier rules, validation
  obligations, and exact apply commands without Odylith making provider calls.
- `odylith greenfield apply --confirm` writes only after confirmation, creates
  accepted backlog records, planned components, release targeting, and
  host-authored Atlas diagrams, then logs accepted intent into Compass memory.
- Science and math prompts receive domain-specific validation obligations such as
  units, tolerances, theorem-checker proof, reproducible notebooks, benchmark
  datasets, reference outputs, and peer-review artifacts without inventing
  scientific claims.
- Consumer and maintainer lanes keep separate host assets and guidance. Codex and
  Claude use native hooks and skills where useful, but the core proposal logic
  lives in the Odylith runtime and CLI.
- The release lane now keeps expensive candidate proof available without running
  the same full pytest and consumer-rehearsal gates repeatedly during GA.
- The local release bundle now carries a matching `0.1.13` wheel, version truth,
  security posture, managed assets, and migration markers for fresh install and
  upgrade proof.

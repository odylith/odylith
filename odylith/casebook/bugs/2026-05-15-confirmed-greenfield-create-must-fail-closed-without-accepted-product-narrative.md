- Bug ID: CB-202

- Status: Open

- Created: 2026-05-15

- Severity: P0

- Reproducibility: Always

- Type: Product

- Description: Confirmed greenfield creation can currently proceed from a thin prompt after Product Intent Confirmation, causing generated Radar, Registry, Atlas, and dashboard records to collapse into generic workflow/state/evidence language instead of preserving the accepted product story, actors, state object, first path, systems, non-goals, and proof boundary.

- Impact: Consumer greenfield projects can receive shallow, generic governance artifacts even after the host wrote a rich Product Intent Confirmation, breaking product understanding before implementation starts.

- Components Affected: greenfield-governance

- Environment(s): Consumer lane pinned release and source-local maintainer validation for v0.1.15 greenfield create path.

- Detected By: Operator review of fresh greenfield generated dashboard and governance records.

- Failure Signature: Generated product story and governance records use generic first workflow/state/evidence scaffold terms instead of the accepted product narrative.

- Trigger Path: Run greenfield propose for a thin or broad new-project prompt, host writes Product Intent Confirmation in chat, then run greenfield create --confirm without passing the confirmed narrative as an input artifact.

- Ownership: Greenfield confirmed-create contract, proposal builder, installed host guidance, and release smoke.

- Timeline: Captured 2026-05-15 through `odylith bug capture`.

- Blast Radius: Any consumer greenfield project, any host model, any domain, and any complexity where the confirmed narrative is not carried into create.

- SLO/SLA Impact: Blocks trustworthy greenfield release proof; confirmed create cannot be considered safe until fail-closed narrative preservation is enforced.

- Data Risk: No application data loss, but generated governance truth can be misleading and can steer implementation from false project understanding.

- Security/Compliance: Domain-specific security, privacy, safety, compliance, and abuse posture can be erased by generic fallback governance language.

- Invariant Violated: Confirmed consumer-lane governance must start from human-readable product understanding and must not write records from a thin prompt after confirmation.

- Root Cause: The confirmed create shortcut moved schema ownership into Odylith but did not require the host-written Product Intent Confirmation as input, so the builder reconstructed records from the prompt title and deterministic generic fallback systems.

- Solution: Require a confirmed-intent artifact for confirmed create/propose-confirmed paths, fail closed when it is missing or shallow, derive components/workstreams/diagrams/project intelligence from that accepted narrative, and make release smoke exercise the same path.

- Rollback/Forward Fix: Forward fix only; prompt-only confirmed create must be rejected rather than tolerated as a compatibility path.

- Verification: Focused CLI tests must show prompt-only create fails, intent-file create preserves domain actors/systems/first path/proof boundary, installed guidance teaches the intent-file path, and release smoke rejects host-side schema repair or generic fallback.

- Prevention: Treat live product narration as a required write input, not chat-only context; add release smoke and quality gates that fail on generic workflow/state/evidence fallback when confirmed domain systems are available.

- Agent Guardrails: Do not hand-author or repair proposal JSON; do not write consumer governance records from a thin prompt; do not leak Odylith artifacts into consumer product story before product meaning is clear.

- Preflight Checks: Before greenfield create writes records, check for product story, state object, first path, human actors, internal systems, and proof boundary from the accepted confirmation.

- Version/Build: v0.1.15

- Config/Flags: consumer lane pinned release; no provider calls required

- Related Incidents/Bugs: CB-173

- GitHub Status: needs_info

- Public Response: pending

## 2026-05-19 Recurrence: Confirmation Format Was Underspecified

- Fresh Failure Signature: The no-write Product Intent Confirmation could be rendered as one large paragraph instead of scannable sections, and normal domain words could surface with decorative Markdown such as code ticks or bold markers. The accepted narrative still contained useful product reasoning, but the transcript lost the operator-facing structure needed for quick review before confirmation.
- Generic Trigger Path: A host follows `greenfield propose`, writes a Product Intent Confirmation in chat, and then asks the operator to confirm. Because the guidance and CLI reasoning payload did not explicitly require sectioned Markdown, the visible confirmation can collapse into prose even when the underlying content is domain-specific.
- Additional Invariant Violated: Before any create/apply write, the visible confirmation must be clear enough for a human operator to verify product story, state object, first path, actors, systems, assumptions, ambiguities, and proof boundary without reconstructing the structure from a paragraph.
- Required Guardrail: The CLI reasoning payload, installed guidance, bundled skills, and release smoke must require a sectioned Product Intent Confirmation: title, Product story, State object, First complete path, Human actors, External systems, Internal product systems, Critical assumptions, Ambiguities, Proof boundary, and Confirm/Edit/Reject. Story/path/proof stay short paragraphs; actors, systems, assumptions, and ambiguities stay bullets; ordinary domain nouns must not be wrapped in code ticks or decorative bold markers.

## 2026-05-19 Recurrence: Fail-Closed Internal Systems Gate Rejected Domain-Specific Evidence Review

- Fresh Failure Signature: Confirmed create rejected a Product Intent Confirmation with `missing or too thin: internal_systems` even after the accepted narrative named concrete product systems. A domain-specific confirmation that included an evidence-review surface reproduced the blocker.
- Trigger Path: The host writes the accepted confirmation to `.odylith/runtime/greenfield/confirmed-intent.md`, then runs `odylith greenfield create --repo-root . --prompt "<request>" --intent-file .odylith/runtime/greenfield/confirmed-intent.md --confirm --release 0.0.1`; `greenfield_confirmed_intent` expands the prose systems but the generic-system detector can mark a domain-specific evidence-review owner as fallback scaffold merely because it contains the words `evidence review`.
- Additional Root Cause: The first guardrail fixed prompt-only writes by making create fail closed, but the internal-systems quality check was over-broad. It treated one generic phrase match as enough to reject the whole accepted product narrative instead of rejecting only the exact generic fallback trio.
- Additional Invariant Violated: The confirmed-create gate must reject missing or generic fallback scaffolds, but it must not reject a domain-specific internal system merely because the system owns evidence review. Evidence review is often a legitimate domain responsibility in reliability, compliance, safety, research, and review workflows.
- Required Guardrail: Preserve the fail-closed prompt-only path while narrowing generic scaffold detection to exact fallback names such as `Workflow Service`, `State Store`, and `Evidence Review` appearing together. Add regression tests that build a domain-specific evidence-review proposal through the confirmed intent parser and greenfield Tribunal, and keep a paired rejection test for the exact generic fallback trio.
- Verification Added: `tests/unit/runtime/test_greenfield_proposals.py::test_confirmed_intent_parser_accepts_domain_specific_evidence_review_surface`, `tests/unit/runtime/test_greenfield_proposals.py::test_confirmed_intent_parser_still_rejects_exact_generic_system_scaffold`, and `tests/unit/test_cli.py::test_greenfield_propose_confirm_intent_json_is_provider_free` passed together (`3 passed`).
- Agent Guardrails: On confirm, do not ask the operator for a second product sentence when the accepted confirmation already carries story, actors, systems, assumptions, risks, and proof boundary. Diagnose the create gate first, preserve the human-visible confirmation as the source of truth, and make the product runtime accept concrete domain systems or return a precise maintainer-grade parser defect.

## 2026-05-20 Recurrence: Confirmed Create Underfilled Governed Artifact Fields

- Fresh Failure Signature: Confirmed create could parse a rich accepted intent and still fail before durable records because generated Registry components did not carry concrete `risks`. After the risk gap was filled, the same path exposed an Atlas refresh failure where newly scaffolded diagrams watched the whole Atlas source tree and therefore marked themselves stale after their own catalog/assets were rendered.
- Generic Trigger Path: In a consumer repo, write the accepted Product Intent Confirmation to `.odylith/runtime/greenfield/confirmed-intent.md`, then run `odylith greenfield create --repo-root . --prompt "<request>" --intent-file .odylith/runtime/greenfield/confirmed-intent.md --confirm --release 0.0.1 --json`.
- Additional Root Cause: The confirmed-create builder relied on downstream component authoring to synthesize risk posture from component text, but the component rows themselves did not carry domain/security/policy risk fields for the governed artifact Tribunal. The diagram completion fallback also used `odylith/atlas/source` as a watch path, which self-invalidated Atlas freshness when the catalog, SVG, and PNG outputs changed.
- Additional Invariant Violated: Confirmation means Odylith owns the complete governed project artifact set. If a field required by Radar, Registry, Atlas, release planning, Compass memory, or dashboard refresh is derivable from the accepted intent, the generator must fill it and rerun gates before writing instead of returning a host-facing blocker.
- Required Guardrail: Keep a confirmed-completion pass between proposal normalization and writes. It must derive missing problem/customer/opportunity/product view, success metrics, backlog risk/security posture, component interfaces/dependencies/validation/risks, diagram watch paths, and project-level security/compliance posture from the accepted intent; run the greenfield Tribunal and governed artifact Tribunals; retry deterministic omissions for a bounded number of passes; and fail only on non-derivable contradictions. Generated Atlas watch paths must not include the generated Atlas source tree itself.
- Verification Added: `tests/unit/runtime/test_greenfield_confirmed_intent.py::test_confirmed_proposal_completion_adds_component_risks_and_fresh_diagram_watch_paths` checks component risk completion, governed component Tribunal pass/fail evidence, and non-self-invalidating diagram watch paths. `tests/unit/runtime/test_greenfield_cli_paths.py::test_greenfield_create_cli_applies_confirmed_prompt` now checks written component specs include domain/security/policy risk posture and created Atlas catalog rows do not watch `odylith/atlas/source`.
- External Repro Proof: A high-risk consumer-repo confirmed-create repro now writes the full governed project set end to end: Radar workstreams, Registry components/specs, Atlas Mermaid/SVG/PNG assets, release assignment, accepted-project memory, and refreshed Radar/Registry/Atlas/Compass/tooling-shell surfaces all pass. No external product-specific labels are copied into Odylith governance truth.
- Agent Guardrails: Upon confirmation, do not ask the operator to supply risk, internal systems, or proof text that is already present or inferable from the accepted intent. Fill the governed fields, run the Tribunals, repair deterministic gaps, and only surface a blocker when the accepted intent truly contradicts itself or omits a non-derivable product decision.

## 2026-05-20 Recurrence: Confirmed Create Stopped On Enrichable Thin Actor And System Rows

- Fresh Failure Signature: Confirmed create could reject an accepted Product Intent Confirmation with `missing or too thin: human_actors` or `missing or too thin: internal_systems` even when the surrounding product story, state object, first path, and proof boundary contained enough domain information to enrich the thin rows safely.
- Generic Trigger Path: The host writes a clear Product Intent Confirmation to `.odylith/runtime/greenfield/confirmed-intent.md`, but the Human actors or Internal product systems sections use concise role or capability names. `odylith greenfield create --repo-root . --prompt "<request>" --intent-file .odylith/runtime/greenfield/confirmed-intent.md --confirm --release 0.0.1` stops before records instead of expanding those rows and rerunning the gates.
- Additional Root Cause: The confirmed-intent parser treated every thin list row as a terminal operator-facing blocker. It did not have a deterministic completion layer that could derive actor responsibility, access posture, product-system responsibility, success metrics, assumptions, ambiguities, and non-goals from the accepted story/path/state/proof context before validation.
- Additional Invariant Violated: After confirmation, Odylith owns completion of derivable governance fields. The host should not be asked for another sentence when the accepted intent already contains enough product evidence to generate clear, grammatical, domain-specific rows and send the result through Tribunal validation.
- Required Guardrail: Complete confirmed intent before validation unless the narrative is missing, meta-scaffolded, or contradictory. The completion layer must stay domain-agnostic, must not store consumer project facts in Odylith governance, must not overwrite already-detailed sections, and must still fail closed for prompt-only or generic scaffold input.
- Verification Added: `tests/unit/runtime/test_greenfield_confirmed_intent.py::test_confirmed_intent_completion_expands_thin_actors_and_systems_generically` proves thin actor/system rows are expanded into reviewable product-language rows. `tests/unit/runtime/test_greenfield_confirmed_intent.py::test_confirmed_greenfield_create_completes_thin_intent_before_governed_records` proves the confirmed CLI create path writes governed records instead of stopping on enrichable thin rows.
- Agent Guardrails: On confirm, do not stop at a deterministic parser gap that can be completed from the accepted intent. Fill derivable sections, rerun validation and Tribunal gates, and surface only non-derivable contradictions or truly missing product narrative.

## 2026-06-02 Recurrence: Semantic Renderers Preserved Parser Debris After Confirmation

- Fresh Failure Signature: Confirmed create could pass the accepted narrative into downstream renderers, then still publish role/action splices, bare outcome nouns, framework proof scaffolds, adjacent-boundary boilerplate, or component-contract fragments that were grammatical enough to pass shape checks but not clear enough for humans to use.
- Generic Trigger Path: A host writes a concrete Product Intent Confirmation to `.odylith/runtime/greenfield/confirmed-intent.md`, then runs `odylith greenfield create --repo-root . --prompt "<request>" --intent-file .odylith/runtime/greenfield/confirmed-intent.md --confirm --release 0.0.1`. The semantic model forms, but first-path clauses, component contracts, workstream text, Atlas labels, and runtime JSON can inherit parser artifacts from intermediate fields.
- Additional Root Cause: The post-confirm path had already moved toward semantic-model-first generation, but several renderers still normalized phrases locally. That let syntactically valid fragments survive as product truth because quality gates checked structure and missing fields more reliably than human-readable semantics.
- Additional Invariant Violated: After confirmation, every generated sentence must trace to accepted intent, derived semantic model facts, or generic governance invariants while staying readable on its own. A valid artifact cannot contain parser debris, activity-shaped actor names, framework proof slogans, or component boundary boilerplate that would be equally plausible in unrelated products.
- Required Guardrail: Keep first-path clause extraction, visible-result detection, component contract differentiation, Atlas label generation, and public prose checks behind semantic quality gates. Repair from the semantic model before writes; fail closed if the repaired package still contains parser artifacts, role/action splices, bare outcome nouns, truncated labels, or generic proof scaffolds.
- Verification Added: `tests/unit/runtime/test_greenfield_post_confirm_slop_regressions.py` covers actor/action splice repair, visible-result clause separation, coordinated action-verb normalization, unheaded intent parsing, runtime JSON debris rejection, component-contract verb normalization, Atlas label hygiene, and public prose slop gates. Replayable proof commands: `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_general_artifact_quality.py tests/unit/runtime/test_greenfield_component_spec_quality.py tests/unit/runtime/test_greenfield_post_confirm_slop_regressions.py tests/unit/runtime/test_greenfield_component_semantic_contract_quality.py tests/unit/runtime/test_greenfield_confirmed_repair.py tests/unit/runtime/test_greenfield_artifact_language_quality.py` (`67 passed`); `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_cli_paths.py` (`17 passed`); `.venv/bin/python -m pytest -q tests/integration/runtime/test_greenfield_create_performance.py` (`1 passed`, under-30s confirmed-create gate with semantic slop check enabled); `.venv/bin/python -m pytest -q tests/integration/runtime/test_project_tab_browser.py` (`2 passed`). Additional temp-repo measurement: `.venv/bin/python /private/tmp/odylith_greenfield_e2e_measure.py --keep` completed in 11.67 seconds with six diagrams, no missing assets, and no slop hits.
- Agent Guardrails: Do not treat shape validation as quality proof. For post-confirm greenfield work, inspect generated Radar, Registry, Atlas, project dashboard, and runtime JSON for human clarity before claiming completion, and keep examples confined to tests rather than Odylith product governance truth.

## 2026-06-03 Follow-up: Confirmed Create Prewrite Gate Needed A Dedicated Owner

- Fresh Finding: The confirmed-create completion orchestrator had grown into a hot-path owner for orchestration, semantic model completion, proposal validation, component/spec preflight checks, greenfield Tribunal execution, and governed-artifact Tribunal issue collection. The behavior was correct, but the file crossed the source-size hard threshold and made future artifact-quality fixes riskier.
- Generic Trigger Path: Maintain `greenfield create --confirm` quality or speed gates after the semantic-render pass. A small prewrite quality change would touch the large completion orchestrator even when the real ownership is Tribunal/preflight aggregation.
- Additional Root Cause: Confirmed-create quality checks were factored by execution order rather than responsibility. The parent completion loop delegated actor repair and text cleanup, but still locally owned the prewrite gate that converts completed proposals into semantic and Tribunal-ready artifacts.
- Additional Invariant Violated: Confirmed-create hot-path code must keep ownership narrow enough that quality gates can improve without reintroducing oversized orchestrators. Source-size discipline is part of the anti-slop contract for this lane.
- Required Guardrail: Keep semantic model completion and all proposal/component/spec/Tribunal preflight issue aggregation in `greenfield_confirmed_prewrite_gate.py`. The parent completion orchestrator may call the gate, but it must not re-own `_artifact_issues`, `run_greenfield_tribunal`, or collector loops for governed-artifact Tribunal results.
- Verification Added: `tests/unit/runtime/test_greenfield_confirmed_repair.py::test_confirmed_completion_prewrite_gate_stays_in_dedicated_owner` pins the owner split, parent line-count ceiling, and Tribunal aggregation location. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_confirmed_completion.py src/odylith/runtime/domain_intelligence/greenfield_confirmed_prewrite_gate.py`; `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_confirmed_repair.py` (`3 passed`); broader greenfield artifact-quality bundle passed 94 tests before Chromium was blocked by sandbox permissions; escalated rerun of `.venv/bin/python -m pytest -q tests/integration/runtime/test_greenfield_create_performance.py` passed (`1 passed in 9.81s`).
- Agent Guardrails: For future confirmed-create artifact-quality work, add behavior in the narrow owner that owns the phase. Do not grow `greenfield_confirmed_completion.py` back into semantic, Tribunal, artifact Tribunal, actor completion, or text-helper ownership.

## 2026-06-03 Follow-up: Confirmed Intent Parser Needed A Dedicated System-Row Owner

- Fresh Finding: The confirmed-intent parser still locally owned JSON/Markdown role rows, internal-system labeled spans, sentence-system splitting, concise system expansion, contextual description repair, and generic scaffold detection. The behavior had accumulated around parser convenience instead of ownership, leaving the file above the source-size hard threshold.
- Generic Trigger Path: Maintain `greenfield create --confirm` input parsing after an operator writes Product Intent Confirmation to `.odylith/runtime/greenfield/confirmed-intent.md`. A small internal-system row parsing change would touch the whole confirmed-intent entrypoint even when the real ownership is system-row normalization.
- Additional Root Cause: The parser had two responsibilities: section/preamble extraction and internal-system row normalization. Actor completion and prewrite gates had dedicated owners, but confirmed-intent system rows still lived inside the entrypoint.
- Additional Invariant Violated: Confirmed-create input parsing must keep syntax parsing, actor completion, system-row normalization, and prewrite quality gates in narrow owners so future artifact-quality fixes do not recreate an oversized parser.
- Required Guardrail: Keep role/system row normalization, labeled-span parsing, sentence-system splitting, system-name prefix detection, generated system-description repair, `confirmed_system_name`, `confirmed_system_description`, and generic scaffold detection in `greenfield_confirmed_system_rows.py`.
- Verification Added: `tests/unit/runtime/test_greenfield_confirmed_intent.py::test_confirmed_intent_system_rows_stay_in_dedicated_owner` pins the owner split and parent line-count ceiling. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_confirmed_intent.py src/odylith/runtime/domain_intelligence/greenfield_confirmed_system_rows.py`; `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_confirmed_intent.py` (`27 passed`); broader greenfield artifact-quality bundle passed (`95 passed in 205.10s`).
- Agent Guardrails: For future confirmed-intent work, route system row parsing and description repair through `greenfield_confirmed_system_rows.py`; do not put row splitting, system-name detection, or generic system scaffold checks back into `greenfield_confirmed_intent.py`.

## 2026-06-03 Follow-up: Post-Confirm Completion Needed A Dedicated Semantic Drift Owner

- Fresh Finding: The post-confirm completion gate still locally owned package orchestration, prewrite-preview fidelity, contrastive domain-drift tokenization, semantic repetition clustering, generated-artifact sentence extraction, and semantic overlap scoring. The behavior was correct but left the file above the source-size hard threshold after the semantic-render hardening pass.
- Generic Trigger Path: Maintain `greenfield create --confirm` post-confirm quality gates after semantic slop checks are enabled. A small change to generated-artifact drift detection would touch the whole package completion gate even when the real ownership is semantic drift scoring.
- Additional Root Cause: The post-confirm gate had split component/spec and first-path semantics into narrower owners, but semantic drift checks still lived at the bottom of the orchestration file because they were originally added as final package checks.
- Additional Invariant Violated: Confirmed-create artifact gates must keep orchestration separate from semantic scoring so future quality fixes do not recreate oversized hot-path modules.
- Required Guardrail: Keep contrastive domain-drift checks, semantic repetition checks, generated-artifact sentence/value extraction, intent/component signature building, and semantic overlap scoring in `greenfield_post_confirm_semantic_drift.py`. The package gate may call these checks, but it must not re-own `_term_signature`, generated-artifact sentence extraction, or local semantic-overlap tokenization.
- Verification Added: `tests/unit/runtime/test_greenfield_general_artifact_quality.py::test_greenfield_post_confirm_semantic_drift_stays_in_dedicated_owner` pins the owner split and parent line-count ceiling. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_post_confirm_completion.py src/odylith/runtime/domain_intelligence/greenfield_post_confirm_semantic_drift.py tests/unit/runtime/test_greenfield_general_artifact_quality.py`; `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_general_artifact_quality.py` (`39 passed in 154.23s`); `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_prewrite_transaction.py` (`22 passed in 108.95s`); broader greenfield artifact-quality bundle passed (`118 passed in 311.35s`); escalated Chromium-capable performance proof passed (`1 passed in 14.83s`).
- Agent Guardrails: For future post-confirm package work, route semantic drift and repetition scoring through `greenfield_post_confirm_semantic_drift.py`; do not put token signature helpers, generated-artifact sentence clustering, or overlap scoring back into `greenfield_post_confirm_completion.py`.

## 2026-06-03 Follow-up: Post-Confirm Completion Needed A Dedicated Semantic Alignment Owner

- Fresh Finding: After semantic drift moved out, the package completion gate still locally owned semantic model shape checks, component/workstream/diagram alignment, rendered Registry spec alignment, component ID fallback, first-release scope checks, and its own row coercion helper. The parent was below the hard threshold but still above the soft source-size limit.
- Generic Trigger Path: Maintain `greenfield create --confirm` post-confirm quality gates when semantic model alignment changes. A small component, workstream, diagram, or rendered-spec alignment fix would still touch the package orchestrator even though the real ownership is semantic model comparison.
- Additional Root Cause: Semantic model alignment was added as a package-gate phase and never split after the confirmed-completion path grew into separate drift, prewrite, and semantic-render owners.
- Additional Invariant Violated: Confirmed-create package orchestration must not own semantic model comparison details or duplicate row coercion helpers after a shared post-confirm row owner exists.
- Required Guardrail: Keep semantic model shape checks, component/workstream/diagram alignment, rendered Registry spec alignment, component ID fallback, and first-release scope checks in `greenfield_post_confirm_semantic_alignment.py`. Keep generated-list row coercion in `greenfield_post_confirm_rows.py`; the parent and drift modules may import that helper but must not redefine `_mapping_rows`.
- Verification Added: `tests/unit/runtime/test_greenfield_general_artifact_quality.py::test_greenfield_post_confirm_semantic_drift_stays_in_dedicated_owner` now pins semantic drift, semantic alignment, and shared row ownership while requiring `greenfield_post_confirm_completion.py` to stay under 800 lines. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_post_confirm_completion.py src/odylith/runtime/domain_intelligence/greenfield_post_confirm_semantic_drift.py src/odylith/runtime/domain_intelligence/greenfield_post_confirm_semantic_alignment.py src/odylith/runtime/domain_intelligence/greenfield_post_confirm_rows.py tests/unit/runtime/test_greenfield_general_artifact_quality.py`; `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_general_artifact_quality.py` (`39 passed in 170.88s`); broader greenfield artifact-quality bundle passed (`118 passed in 334.66s`); escalated Chromium-capable performance proof passed (`1 passed in 11.96s`).
- Agent Guardrails: For future post-confirm package work, route semantic model alignment and rendered-spec alignment through `greenfield_post_confirm_semantic_alignment.py`; do not put semantic shape comparison, component/workstream/diagram alignment, first-release scope checks, or row coercion clones back into `greenfield_post_confirm_completion.py`.

## 2026-06-03 Follow-up: Confirmed Intent Parser Needed A Dedicated Validation Owner

- Fresh Finding: After system-row parsing moved out, the confirmed-intent parser still locally owned field thresholds, meta-narration rejection, qualitative gap checks, progression/outcome scoring, and semantic-overlap term extraction. The parser was below the hard threshold but still above the soft source-size limit.
- Generic Trigger Path: Maintain `greenfield create --confirm` input validation after an operator writes Product Intent Confirmation to `.odylith/runtime/greenfield/confirmed-intent.md`. A small validation quality change would still touch the parser, even though the real ownership is fail-closed accepted-intent validation.
- Additional Root Cause: Confirmed-intent validation originally grew inside the parser because both Markdown parsing and validation need title/story/path/proof fields. After actor and system owners landed, the validation gate remained the last major non-parser responsibility in the file.
- Additional Invariant Violated: Confirmed-intent parsing must stay separate from accepted-intent validation so file loading, section parsing, and preamble derivation do not own semantic quality thresholds or meta-scaffold rejection.
- Required Guardrail: Keep field-threshold checks, meta-narration detection, qualitative gap checks, progression/outcome scoring, semantic overlap, and term extraction in `greenfield_confirmed_intent_validation.py`. The parser may call `validate_confirmed_intent` and use the shared field thresholds for preamble derivation, but it must not re-own validation internals.
- Verification Added: `tests/unit/runtime/test_greenfield_confirmed_intent.py::test_confirmed_intent_system_rows_stay_in_dedicated_owner` now pins validation ownership and requires `greenfield_confirmed_intent.py` to stay under 800 lines. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_confirmed_intent.py src/odylith/runtime/domain_intelligence/greenfield_confirmed_intent_validation.py tests/unit/runtime/test_greenfield_confirmed_intent.py`; `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_confirmed_intent.py` (`27 passed in 41.27s`); broader greenfield artifact-quality bundle passed (`118 passed in 349.90s`); escalated Chromium-capable performance proof passed (`1 passed in 11.79s`).
- Agent Guardrails: For future confirmed-intent work, route accepted-intent validation through `greenfield_confirmed_intent_validation.py`; do not put qualitative gap logic, semantic term extraction, or meta-narration pattern checks back into `greenfield_confirmed_intent.py`.

## 2026-06-03 Follow-up: Confirmed Completion Needed A Shared Quality Predicate Owner

- Fresh Finding: The confirmed-completion orchestrator still locally owned text repair detection, sequence repair detection, proof-boundary weakness checks, public-prose quality integration, and bad sentence-tail detection. These predicates are reused by project posture, backlog completion, component repair, generated-sentence repair, project-intelligence completion, and preflight repair routing.
- Generic Trigger Path: Maintain `greenfield create --confirm` repair quality when generated text, success metrics, validation, or component prose contains parser debris. A small quality predicate change would touch the whole confirmed-completion orchestrator even when the real ownership is generic text quality.
- Additional Root Cause: Text quality checks grew inside the completion loop because they were first added as repair triggers. Later phases reused them, but no shared owner existed for completion-time quality predicates.
- Additional Invariant Violated: Confirmed-completion orchestration must not own generic quality predicates. Shared quality detection must be reusable across completion phases without growing the orchestrator.
- Required Guardrail: Keep text repair detection, sequence repair detection, proof-boundary weakness checks, public-prose quality integration, and bad sentence-tail detection in `greenfield_confirmed_completion_quality.py`. The completion orchestrator may import those predicates, but it must not re-own `_text_needs_repair`, `_sequence_needs_repair`, or `_has_bad_tail`.
- Verification Added: `tests/unit/runtime/test_greenfield_confirmed_repair.py::test_confirmed_completion_prewrite_gate_stays_in_dedicated_owner` now pins the quality owner split. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_confirmed_completion.py src/odylith/runtime/domain_intelligence/greenfield_confirmed_completion_quality.py tests/unit/runtime/test_greenfield_confirmed_repair.py`; `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_confirmed_repair.py` (`3 passed in 10.66s`); broader greenfield artifact-quality bundle passed (`118 passed in 332.17s`); escalated Chromium-capable performance proof passed (`1 passed in 11.54s`).
- Agent Guardrails: For future confirmed-completion work, route text and sequence quality checks through `greenfield_confirmed_completion_quality.py`; do not put public-prose quality integration, sentence-tail checks, or sequence token requirements back into `greenfield_confirmed_completion.py`.

## 2026-06-03 Follow-up: Confirmed Completion Needed A Shared Text Model Owner

- Fresh Finding: The confirmed-completion orchestrator still locally owned action/outcome phrasing, project/component/diagram labels, workstream sentence templates, first-path/proof/state summaries, actor summaries, keyword extraction, and backlog-to-component lexical matching. That made the repair loop the owner of both mutation order and the text model used by backlog, component reconciliation, diagrams, project posture, and generated-sentence repair.
- Generic Trigger Path: Maintain `greenfield create --confirm` artifact quality when action phrasing, component labels, workstream subjects, diagram titles, or backlog/component traceability drift. A small text-model correction should not require editing the confirmed-completion orchestration loop.
- Additional Root Cause: The phrase helpers grew beside the completion repairs because the first failing artifacts were missing prose. Once those helpers became shared by backlog, component, diagram, and release proof repair, their ownership no longer matched the orchestrator.
- Additional Invariant Violated: Confirmed-completion orchestration must not own reusable phrase, label, keyword, or lexical matching rules. Those rules need a shared owner so later backlog or component owner splits do not clone text helpers.
- Required Guardrail: Keep confirmed-completion phrase derivation, labels, first-path/proof/state summaries, actor summaries, keywords, and backlog-to-component matching in `greenfield_confirmed_completion_text_model.py`. The completion orchestrator may call that module directly, but it must not re-own `_project_title`, `_component_label`, `_keywords`, `_action_phrase`, or `_primary_component_for_backlog`.
- Verification Added: `tests/unit/runtime/test_greenfield_confirmed_repair.py::test_confirmed_completion_prewrite_gate_stays_in_dedicated_owner` now pins the text-model owner split, requires `greenfield_confirmed_completion.py` to stay below 800 lines, and rejects dead component wrapper helpers. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_confirmed_completion.py src/odylith/runtime/domain_intelligence/greenfield_confirmed_completion_text_model.py tests/unit/runtime/test_greenfield_confirmed_repair.py`; `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_confirmed_repair.py` (`3 passed in 9.95s`); broader greenfield artifact-quality bundle passed (`118 passed in 310.95s`); confirmed-create performance proof passed (`1 passed in 10.08s`).
- Agent Guardrails: For future confirmed-completion work, route phrase and lexical matching changes through `greenfield_confirmed_completion_text_model.py`; do not add pass-through component wrappers or local text helper shims back into `greenfield_confirmed_completion.py`.

## 2026-06-03 Follow-up: Confirmed Backlog Needed A Shared Radar Text Model Owner

- Fresh Finding: The confirmed Radar backlog builder still locally owned actor label extraction, first-action and outcome clauses, proof focus selection, program-problem fallback wording, product-term overlap checks, and rationale-line generation. That made the workstream record assembler responsible for both data assembly and phrase grammar.
- Generic Trigger Path: Maintain `greenfield create --confirm` Radar artifact quality when workstream titles, Problem, Opportunity, Product View, Success Metrics, Recommended First Slice, or Rationale Lines drift toward generic filler or awkward action grammar.
- Additional Root Cause: The backlog builder originally generated four rows directly, so helper phrases accumulated beside the row templates. After completion and component owners split out, those text helpers became reusable artifact-quality rules and no longer belonged inside the row assembler.
- Additional Invariant Violated: Confirmed Radar workstream assembly must not own reusable phrase, proof, actor-label, or rationale grammar rules. Those rules need a text-model owner so future workstream quality changes do not grow the row builder back over the soft limit.
- Required Guardrail: Keep confirmed-backlog phrase derivation, actor labels, first-action/outcome clauses, proof focus selection, problem fallback wording, product-term overlap checks, and rationale-line generation in `greenfield_confirmed_backlog_text_model.py`. The backlog row builder may call that module, but it must not re-own `_program_problem`, `_sentence_fragment`, or `_first_action_clause`.
- Verification Added: `tests/unit/runtime/test_greenfield_artifact_language_quality.py::test_confirmed_backlog_text_model_stays_in_dedicated_owner` now pins the text-model owner and requires `greenfield_confirmed_backlog.py` to stay below 800 lines. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_confirmed_backlog.py src/odylith/runtime/domain_intelligence/greenfield_confirmed_backlog_text_model.py src/odylith/runtime/domain_intelligence/greenfield_confirmed_proposal.py tests/unit/runtime/test_greenfield_component_spec_quality.py tests/unit/runtime/test_greenfield_artifact_language_quality.py`; `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_artifact_language_quality.py tests/unit/runtime/test_greenfield_component_spec_quality.py` (`15 passed in 3.04s`); broader greenfield artifact-quality bundle passed (`120 passed in 311.68s`); confirmed-create performance proof passed (`1 passed in 10.09s`).
- Agent Guardrails: For future confirmed backlog work, route phrase and rationale changes through `greenfield_confirmed_backlog_text_model.py`; do not add private text-helper shims back into `greenfield_confirmed_backlog.py`.

## 2026-06-03 Follow-up: Confirmed Diagrams Needed A Shared Text And Label Owner

- Fresh Finding: The confirmed Atlas diagram builder still locally owned component card descriptions, product and proof briefs, workstream title derivation, Mermaid label trimming, proof-review labels, and short-label cleanup alongside diagram row assembly and flowchart wiring. The behavior was correct, but the file stayed above the 800-line soft limit after the backlog text-model split.
- Generic Trigger Path: Maintain `greenfield create --confirm` Atlas artifact quality when diagram summaries, component cards, proof-review labels, state/evidence labels, or Mermaid node labels drift toward boilerplate or clipped grammar.
- Additional Root Cause: Confirmed diagram generation had been factored by output file rather than phase. Text and label helpers accumulated beside the six diagram row templates because Atlas was the first surface to expose several generated-copy defects.
- Additional Invariant Violated: Confirmed Atlas diagram assembly must not own reusable text and label grammar. The row builder should compose diagram records and flowchart topology while a dedicated text model owns prose, proof labels, component descriptions, and Mermaid-safe short labels.
- Required Guardrail: Keep confirmed-diagram component descriptions, briefs, proof labels, workstream title derivation, label trimming, and short-label cleanup in `greenfield_confirmed_diagram_text.py`. The diagram builder may call that owner, but it must not re-own `_component_description`, `_brief_proof_boundary`, or `_short_label`.
- Verification Added: `tests/unit/runtime/test_greenfield_confirmed_diagrams.py::test_confirmed_diagram_text_model_stays_in_dedicated_owner` now pins the text-model owner and requires `greenfield_confirmed_diagrams.py` to stay below 800 lines. The stale confirmed-surface assertion now expects the current imperative action label `Tap Record`. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_confirmed_diagrams.py src/odylith/runtime/domain_intelligence/greenfield_confirmed_diagram_text.py tests/unit/runtime/test_greenfield_confirmed_diagrams.py tests/unit/runtime/test_greenfield_confirmed_surfaces.py`; `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_confirmed_diagrams.py tests/unit/runtime/test_greenfield_confirmed_surfaces.py` (`6 passed in 5.80s`); broader greenfield artifact-quality bundle passed (`126 passed in 388.79s`); confirmed-create performance proof passed (`1 passed in 14.50s`).
- Agent Guardrails: For future confirmed Atlas work, route prose and label grammar changes through `greenfield_confirmed_diagram_text.py`; do not add private component-description, proof-label, or label-trimming helpers back into `greenfield_confirmed_diagrams.py`.

## 2026-06-03 Follow-up: Registry Component Contracts Needed Dedicated Profile Owners

- Fresh Finding: The generated Registry component contract owner still mixed public contract APIs, generic fallback contract generation, document-context profile grammar, and status-view profile grammar in one file. The behavior was correct, but Registry artifact-quality changes for document uploads or lifecycle status views would touch the full contract owner and keep it above the 800-line soft limit.
- Generic Trigger Path: Maintain `greenfield create --confirm` Registry component spec quality when generated component contracts need document-context, attachment/evidence packet, status timeline, role visibility, stale indicator, or lifecycle transition wording.
- Additional Root Cause: Component contracts were originally generated as one deterministic helper, then profile-specific rules accumulated inside it as quality gates rejected generic Registry specs. Those profiles became separate policy owners, but they remained inside the parent module.
- Additional Invariant Violated: Generated Registry component contract assembly must keep profile selection, generic fallback contracts, and specialized profile grammar separate. Profile-specific proof rows and outside-boundary wording should not make the parent contract owner grow back over the soft limit.
- Required Guardrail: Keep document-context and status-view profile contract builders, phrase extraction, transition selection, outside-boundary wording, and local proof rows in `greenfield_component_contract_profiles.py`. `greenfield_component_contract.py` may select and call the profile owner, but it must not re-own `_document_context_contract`, `_status_view_contract`, `_document_local_proof`, or `_status_local_proof`.
- Verification Added: `tests/unit/runtime/test_greenfield_component_spec_quality.py::test_component_contract_profiles_stay_in_dedicated_owner` now pins the profile owner and requires `greenfield_component_contract.py` to stay below 800 lines. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_component_contract.py src/odylith/runtime/domain_intelligence/greenfield_component_contract_profiles.py tests/unit/runtime/test_greenfield_component_spec_quality.py`; `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_component_spec_quality.py tests/unit/runtime/test_greenfield_component_semantic_contract_quality.py` (`10 passed in 0.40s`); broader greenfield artifact-quality bundle passed (`127 passed in 403.37s`); confirmed-create performance proof passed (`1 passed in 13.49s`).
- Agent Guardrails: For future generated Registry component contract work, route document-context and status-view profile grammar through `greenfield_component_contract_profiles.py`; do not put specialized profile proof rows or outside-boundary helpers back into `greenfield_component_contract.py`.

## 2026-06-03 Follow-up: Registry Component Semantic Contracts Needed A Context Owner

- Fresh Finding: The generated Registry semantic contract owner still mixed contract assembly with context-derived phrase extraction, late first-path/proof backfill selection, context anchor expansion, and actor/action prefix cleanup. The file stayed above the 800-line soft limit even after component contract profile extraction.
- Generic Trigger Path: Maintain `greenfield create --confirm` Registry component spec quality when component descriptions are thin and the semantic contract must recover specific artifacts, measurements, timelines, status markers, or proof obligations from accepted product intent context.
- Additional Root Cause: Context backfill began as a repair path for thin generated component rows, then became a full phrase-selection model. It remained inside the semantic contract assembler because both phases share label and description terms, but its ownership is context extraction rather than contract rendering.
- Additional Invariant Violated: Generated Registry semantic contract assembly must not own context phrase mining or actor/action prefix filtering. Context extraction should be reusable and test-pinned so future Registry artifact-quality fixes do not grow the assembler above the soft limit.
- Required Guardrail: Keep context-derived phrase extraction, context-required phrase ranking, anchor expansion, context-backfill decisions, and actor/action prefix cleanup in `greenfield_component_semantic_context.py`. `greenfield_component_semantic_contract.py` may call that owner, but it must not re-own `_context_object_phrases`, `_context_required_phrases`, or `_needs_context_backfill`.
- Verification Added: `tests/unit/runtime/test_greenfield_component_semantic_contract_quality.py::test_component_semantic_context_stays_in_dedicated_owner` now pins the context owner and requires `greenfield_component_semantic_contract.py` to stay below 800 lines. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_component_semantic_contract.py src/odylith/runtime/domain_intelligence/greenfield_component_semantic_context.py tests/unit/runtime/test_greenfield_component_semantic_contract_quality.py`; `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_component_semantic_contract_quality.py tests/unit/runtime/test_greenfield_component_spec_quality.py` (`11 passed in 0.41s`); broader greenfield artifact-quality bundle passed (`128 passed in 345.24s`); confirmed-create performance proof passed (`1 passed in 11.45s`).
- Agent Guardrails: For future generated Registry semantic contract work, route context phrase and backfill changes through `greenfield_component_semantic_context.py`; do not put context phrase mining, actor/action prefix cleanup, or context anchor expansion back into `greenfield_component_semantic_contract.py`.

## 2026-06-03 Follow-up: Registry Component Contract Differentiation Needed A Target Owner

- Fresh Finding: The generated Registry component contract differentiation owner still mixed contract repair orchestration with rendered-spec issue parsing, repair-target row selection, duplicate target dedupe, and operator-facing component-spec blocker copy. The behavior was correct, but the file stayed above the 800-line soft limit after the semantic-context split.
- Generic Trigger Path: Maintain `greenfield create --confirm` Registry component spec quality when rendered component specs remain interchangeable or lack component-local terms after deterministic repair.
- Additional Root Cause: Rendered-spec target parsing was introduced beside the repair loop because both phases run during differentiation. Once component contract and semantic contract owners split, target parsing became a separate preflight/repair-selection responsibility.
- Additional Invariant Violated: Generated Registry component contract differentiation must not own rendered-spec issue parsing or operator blocker wording. Target selection should be reusable and test-pinned so future Registry artifact-quality fixes do not grow the repair orchestrator above the soft limit.
- Required Guardrail: Keep rendered-spec issue parsing, `RepairTarget`, duplicate target dedupe, and operator component-spec blocker copy in `greenfield_component_contract_targets.py`. `greenfield_component_contract_differentiation.py` may call that owner, but it must not re-own `_repair_targets`, `_operator_issue`, `_dedupe_targets`, or a private repair-target dataclass.
- Verification Added: `tests/unit/runtime/test_greenfield_component_spec_quality.py::test_component_contract_targets_stay_in_dedicated_owner` pins the target owner, requires `greenfield_component_contract_differentiation.py` to stay below 800 lines, and verifies concrete issue-to-row/sibling mapping. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_component_contract_differentiation.py src/odylith/runtime/domain_intelligence/greenfield_component_contract_targets.py src/odylith/runtime/domain_intelligence/greenfield_proposals.py src/odylith/runtime/domain_intelligence/greenfield_post_confirm_completion.py tests/unit/runtime/test_greenfield_component_spec_quality.py tests/unit/runtime/test_greenfield_confirmed_body_comp.py tests/unit/runtime/test_greenfield_confirmed_repair.py`; `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_component_spec_quality.py tests/unit/runtime/test_greenfield_confirmed_body_comp.py tests/unit/runtime/test_greenfield_confirmed_repair.py` (`15 passed in 14.63s`); broader greenfield artifact-quality bundle passed (`129 passed in 332.92s`); confirmed-create performance proof passed (`1 passed in 11.39s`).
- Agent Guardrails: For future generated Registry component-spec repair work, route rendered-spec issue parsing and operator blocker copy through `greenfield_component_contract_targets.py`; do not add private target parsing, dedupe, or component-spec issue wording helpers back into `greenfield_component_contract_differentiation.py`.

## 2026-06-03 Follow-up: Apply Prewrite Needed Component And Diagram Owners

- Fresh Finding: The confirmed-create apply-prewrite owner still mixed staged package assembly, release preview, accepted-project memory preview, Compass preview, Registry component input shaping, dry-run component preview, in-memory Registry spec rendering, component dependency/risk/responsibility copy, Atlas Mermaid preview, diagram ID allocation, and path remapping. The behavior was correct, but the file stayed above the 800-line soft limit and put Radar/Registry quality fixes in a broad orchestration module.
- Generic Trigger Path: Maintain `greenfield create --confirm` prewrite package quality when Registry components, component specs, Atlas source previews, diagram IDs, or package gates change before governed writes.
- Additional Root Cause: Prewrite originally grew as one package gate, so preview rendering and path staging accumulated in one file. After confirmed-create completion and Registry component-contract owners split, the Registry/Atlas preview phases no longer matched the package assembler's responsibility.
- Additional Invariant Violated: Confirmed-create prewrite orchestration must not own Registry component rendering policy or Atlas preview/id allocation. Those phases should be reusable and test-pinned so future artifact-quality fixes do not grow the package assembler back over the soft limit.
- Required Guardrail: Keep first-release component input shaping, dry-run component previews, in-memory Registry spec rendering, component dependency/risk/responsibility copy, Atlas preview rendering, and diagram ID allocation in `greenfield_apply_components.py` and `greenfield_apply_diagrams.py`. `greenfield_apply_prewrite.py` may call those owners, but it must not re-own `render_prewrite_component_specs`, `preview_prewrite_components`, `component_authoring_prewrite_inputs`, `component_risk_lines`, `allocated_diagram_ids`, or `render_prewrite_atlas_sources`.
- Verification Added: `tests/unit/runtime/test_greenfield_prewrite_transaction.py::test_greenfield_apply_prewrite_component_and_diagram_phases_stay_dedicated` pins the component and diagram owners and requires `greenfield_apply_prewrite.py` to stay below 800 lines. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_apply_prewrite.py src/odylith/runtime/domain_intelligence/greenfield_apply_components.py src/odylith/runtime/domain_intelligence/greenfield_apply_diagrams.py src/odylith/runtime/domain_intelligence/greenfield_proposals.py tests/unit/runtime/test_greenfield_prewrite_transaction.py tests/unit/runtime/test_greenfield_general_artifact_quality.py tests/unit/runtime/test_greenfield_proposals.py`; `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_prewrite_transaction.py tests/unit/runtime/test_greenfield_proposals.py::test_greenfield_component_dependency_lines_are_grammatical_from_component_rows tests/unit/runtime/test_greenfield_general_artifact_quality.py::test_greenfield_apply_post_confirm_failure_is_internal_not_operator_repair` (`25 passed in 124.26s`); broader greenfield artifact-quality bundle passed (`130 passed in 333.90s`); confirmed-create performance proof passed (`1 passed in 10.93s`).
- Agent Guardrails: For future confirmed-create prewrite work, route Registry component preview/spec input changes through `greenfield_apply_components.py` and Atlas prewrite source/id changes through `greenfield_apply_diagrams.py`; do not add Registry rendering, component posture, dependency grammar, diagram allocation, or Atlas preview helpers back into `greenfield_apply_prewrite.py`.

## 2026-06-03 Follow-up: Final Apply Needed A Governed Write Owner

- Fresh Finding: The confirmed-create proposal entrypoint still owned the last write phase after prewrite package decomposition. It carried Radar file writes, stale workstream cleanup, release targeting writes, program creation, Atlas scaffold/upsert behavior, Registry component authoring, accepted-project memory recording, dashboard refresh, and next-step shaping alongside CLI parsing and proposal repair.
- Generic Trigger Path: Maintain `greenfield create --confirm` final apply behavior when Radar, Registry, Atlas, release targeting, accepted-project memory, dashboard refresh, or next-step outputs change after the prewrite package passes.
- Additional Root Cause: Confirmed apply had been split at the prewrite/package boundary but not at the final write boundary. The parent module delegated package previews yet still owned every durable source-truth write, so a Registry or Atlas write-quality fix could grow the user-facing entrypoint back into a broad orchestrator.
- Additional Invariant Violated: Confirmed-create entrypoints must not own durable governed writes after the prewrite package is accepted. Final writes need a dedicated owner so transaction setup, proposal repair, and CLI output stay separate from Radar/Registry/Atlas/release/memory mutations.
- Required Guardrail: Keep final governed writes in `greenfield_apply_write.py`. `greenfield_proposals.py` may call `release_assignment_note` and `write_greenfield_proposal`, but it must not re-own Radar source writes, stale artifact cleanup, release assignment writes, program wave creation, Atlas scaffold/upsert helpers, Registry component authoring, accepted-project memory recording, dashboard refresh, or next-step shaping.
- Verification Added: `tests/unit/runtime/test_greenfield_proposals.py::test_greenfield_apply_write_stays_in_dedicated_owner` pins the final-write owner and requires `greenfield_proposals.py` to stay below 800 lines. The pass also fixed a semantic gate edge where blank component `release_scope` was treated as inactive by the DiagramEventGraph alignment gate even though the semantic builder and Registry writer treat blank scope as active; apply-time semantic repair now refreshes stale semantic models before the completion gate. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_apply_semantic.py src/odylith/runtime/domain_intelligence/greenfield_apply_write.py src/odylith/runtime/domain_intelligence/greenfield_post_confirm_semantic_alignment.py src/odylith/runtime/domain_intelligence/greenfield_proposals.py tests/unit/runtime/test_greenfield_proposals.py tests/unit/runtime/test_greenfield_prewrite_transaction.py tests/unit/runtime/test_greenfield_general_artifact_quality.py`; `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_proposals.py tests/unit/runtime/test_greenfield_prewrite_transaction.py tests/unit/runtime/test_greenfield_general_artifact_quality.py::test_greenfield_apply_post_confirm_failure_is_internal_not_operator_repair` (`68 passed in 141.47s`); broader greenfield artifact-quality bundle passed (`130 passed in 331.45s`); confirmed-create performance proof passed (`1 passed in 11.41s`).
- Agent Guardrails: For future final apply work, route source-truth mutation changes through `greenfield_apply_write.py`; do not add write helpers, imported governance writers, Atlas scaffold logic, Registry component authoring calls, or dashboard refresh calls back into `greenfield_proposals.py`.

## 2026-06-03 Follow-up: First Path Semantics Needed A Clause Rendering Owner

- Fresh Finding: The first-path semantics module still owned both accepted-intent parsing and generated action/capability/outcome clause rendering. The behavior was correct, but a parser fix could touch Radar, Registry, Atlas, runtime JSON, and dashboard prose grammar in the same large file.
- Generic Trigger Path: Maintain `greenfield create --confirm` artifact quality when first-path action text, visible-result wording, trivial-start detection, or action-chain grammar drifts toward parser debris or generic filler.
- Additional Root Cause: First-path helpers originally accumulated around the parser because semantic slop failures surfaced as malformed first-path text. Once those helpers fed several generated surfaces, their real ownership became rendering and cleanup, not model extraction.
- Additional Invariant Violated: First-path parsing must not own generated clause rendering. Parser/model extraction, clause grammar, and shared typed records need separate owners so future Radar or Registry copy fixes do not grow the parser back over the soft limit.
- Required Guardrail: Keep first-path parsing and model extraction in `greenfield_first_path_semantics.py`; keep action/capability/outcome clause rendering, visible-result cleanup, trivial-start detection, and action-chain grammar in `greenfield_first_path_clauses.py`; keep `FirstPathModel` and `FirstPathClauses` in `greenfield_first_path_types.py`.
- Verification Added: `tests/unit/runtime/test_greenfield_post_confirm_slop_regressions.py::test_first_path_clause_rendering_stays_in_dedicated_owner` pins the owner boundary and requires the parser to stay below 800 lines. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_first_path_semantics.py src/odylith/runtime/domain_intelligence/greenfield_first_path_clauses.py src/odylith/runtime/domain_intelligence/greenfield_first_path_types.py src/odylith/runtime/domain_intelligence/greenfield_semantic_quality.py tests/unit/runtime/test_greenfield_post_confirm_slop_regressions.py`; `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_post_confirm_slop_regressions.py` (`14 passed in 2.73s`); broader greenfield artifact-quality bundle passed (`61 passed in 174.67s`); full confirmed-greenfield regression bundle passed (`131 passed in 322.94s`); confirmed-create performance proof passed (`1 passed in 10.11s`).
- Agent Guardrails: For future first-path work, route generated action, capability, visible-result, action-chain, and cleanup grammar through `greenfield_first_path_clauses.py`; do not add local clause-rendering helpers back into `greenfield_first_path_semantics.py`.

## 2026-06-03 Follow-up: Tribunal Artifact Substance Needed A Dedicated Owner

- Fresh Finding: The deterministic greenfield Tribunal still mixed top-level prewrite adjudication with confirmed Radar, Registry, and Atlas artifact-substance checks. The behavior was correct, but the file carried release topology, workstream/component/diagram traceability, security posture, visible actor checks, generated Radar thinness checks, Registry proof-leakage checks, and Atlas first-path quality gates together.
- Generic Trigger Path: Maintain `greenfield create --confirm` when confirmed generated workstreams, component contracts, Atlas diagrams, or Tribunal evidence drift toward generic prose, cross-axis proof leakage, scaffold nodes, first-path tail loss, or wrong first-boundary routing.
- Additional Root Cause: Confirmed artifact-substance checks grew inside the Tribunal because they were initially Tribunal issues. Once they became a full generated-artifact quality layer spanning Radar, Registry, and Atlas, their ownership no longer matched the prewrite adjudication parent.
- Additional Invariant Violated: The greenfield Tribunal parent must orchestrate deterministic prewrite adjudication without owning every generated-artifact substance heuristic. Radar, Registry, and Atlas specificity checks need a dedicated owner so Tribunal improvements do not grow the adjudicator back over the soft limit.
- Required Guardrail: Keep confirmed Radar thinness checks, Registry component-contract substance checks, cross-axis proof leakage checks, Atlas scaffold-node rejection, first-path tail preservation, first-boundary routing, and project-term specificity analysis in `proposal_tribunal_substance.py`. `proposal_tribunal.py` may call `check_confirmed_artifact_substance`, but it must not re-own those helpers.
- Verification Added: `tests/unit/runtime/test_greenfield_general_artifact_quality.py::test_greenfield_tribunal_substance_gate_stays_in_dedicated_owner` pins the owner boundary and requires `proposal_tribunal.py` to stay below 800 lines. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/proposal_tribunal.py src/odylith/runtime/domain_intelligence/proposal_tribunal_substance.py tests/unit/runtime/test_greenfield_general_artifact_quality.py`; `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_general_artifact_quality.py -k 'tribunal or post_confirm_semantic_drift'` (`5 passed in 10.58s`); broader greenfield artifact-quality bundle passed (`62 passed in 165.34s`); full confirmed-greenfield regression bundle passed (`132 passed in 315.92s`); confirmed-create performance proof passed (`1 passed in 9.77s`).
- Agent Guardrails: For future Tribunal artifact-quality work, route generated Radar, Registry, and Atlas substance heuristics through `proposal_tribunal_substance.py`; do not add Radar thinness, Registry proof-leakage, Atlas first-path tail, or project-term specificity helpers back into `proposal_tribunal.py`.

## 2026-06-03 Follow-up: Confirmed Intent Needed A System Completion Owner

- Fresh Finding: The confirmed-intent completion orchestrator still owned internal system row completion, fallback system generation, system label repair, state-label extraction, and context-clause matching alongside core-field completion, product posture, title repair, first-path wording, and proof-boundary wording.
- Generic Trigger Path: Maintain `greenfield create --confirm` when accepted internal product systems are concise, missing descriptions, or need fallback generation from the product story, state object, first path, and proof boundary before Registry component generation begins.
- Additional Root Cause: System completion stayed in the parent after system row parsing moved to `greenfield_confirmed_system_rows.py`. That left parsing ownership separated, but generated system completion still lived beside unrelated core-field and product-posture repair.
- Additional Invariant Violated: Confirmed-intent completion must not own the internal-system completion phase. Product systems drive generated Registry components, Atlas boundaries, and Radar workstreams, so their fallback generation and cleanup need a dedicated owner with structural tests.
- Required Guardrail: Keep internal system row completion, fallback system generation, system label cleanup, state-label extraction, and context-clause matching in `greenfield_confirmed_system_completion.py`. `greenfield_confirmed_intent_completion.py` may call `completed_system_rows`, `system_labels`, and `state_label`, but it must not re-own the private system-completion helpers.
- Verification Added: `tests/unit/runtime/test_greenfield_confirmed_intent.py::test_confirmed_intent_system_completion_stays_in_dedicated_owner` pins the owner boundary and requires `greenfield_confirmed_intent_completion.py` to stay below 800 lines. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_confirmed_intent_completion.py src/odylith/runtime/domain_intelligence/greenfield_confirmed_system_completion.py tests/unit/runtime/test_greenfield_confirmed_intent.py`; `.venv/bin/python -m pytest -q tests/unit/runtime/test_greenfield_confirmed_intent.py` (`28 passed in 39.53s`); broader greenfield artifact-quality bundle passed (`90 passed in 225.71s`); full confirmed-greenfield regression bundle passed (`133 passed in 341.80s`); confirmed-create performance proof passed (`1 passed in 12.05s`).
- Agent Guardrails: For future confirmed-intent system work, route generated internal-system labels, fallback systems, state labels, and context matching through `greenfield_confirmed_system_completion.py`; do not add private system completion helpers back into `greenfield_confirmed_intent_completion.py`.

## 2026-06-03 Follow-up: Atlas First-Path Rendering Needed A Step Derivation Owner

- Fresh Finding: The Atlas sequence renderer still owned semantic event extraction, launcher-only filtering, first-path fallback parsing, compound-step expansion, step dedupe, participant routing, component routing, Mermaid label cleanup, and message rendering in one file. The file was above the 800-line soft limit, and the full confirmed suite exposed a coupled tail bug: a valid first-path decision ending in `act later` was classified as deferred scope and dropped from the First Path Sequence.
- Generic Trigger Path: Maintain `greenfield create --confirm` Atlas output when an accepted first path contains a final decision such as `research, ignore, or act later`. A rendering refactor must not drop that final action or force the Tribunal tail-preservation gate to fail closed.
- Additional Root Cause: First-path step derivation was embedded in the renderer, so behavior fixes for event extraction and fallback parsing were coupled to Mermaid routing. The shared artifact cleaner also stripped short role-qualified artifact labels after the tail fix let the affected title-quality fixture reach component assertions.
- Additional Invariant Violated: Atlas renderers must not own accepted-event derivation. Confirmed-create must preserve the final user-visible path action and keep component-local Registry artifacts such as `person follow list` and `reviewer notes` intact.
- Required Guardrail: Keep semantic event extraction, launcher-only filtering, first-path fallback parsing, compound-step expansion, and step dedupe in `greenfield_sequence_steps.py`. Keep participant/component routing and Mermaid rendering in `greenfield_sequence_diagram.py`. Scope/deferred detection must not reject action-later user decisions, and artifact cleanup must preserve short role-qualified artifact identities that end in owned artifact nouns.
- Verification Added: `tests/unit/runtime/test_greenfield_confirmed_diagrams.py::test_sequence_event_steps_stay_in_dedicated_owner` pins the owner split and renderer line-count ceiling. `tests/unit/runtime/test_greenfield_confirmed_diagrams.py::test_sequence_event_steps_preserve_action_later_decision_tail` pins the action-later tail. Replayable proof: focused diagram/sequence proof passed (`11 passed, 36 deselected in 29.77s`); full confirmed-greenfield proof passed (`48 passed in 69.77s`); broader greenfield artifact-quality bundle passed (`98 passed in 244.93s`); confirmed-create performance proof passed (`1 passed in 12.44s`).
- Agent Guardrails: For future Atlas first-path work, route step derivation through `greenfield_sequence_steps.py`; do not put event parsing, fallback first-path splitting, launcher filtering, or dedupe back into `greenfield_sequence_diagram.py`. For Registry artifact cleanup, preserve compact role-qualified artifact labels instead of treating every role word as disposable noise.

## 2026-06-03 Follow-up: Artifact Enrichment Needed Graph And Tribunal Actor Owners

- Fresh Finding: `artifact_enrichment.py` still owned Domain Intelligence graph normalization, workstream artifact projections, and visible Tribunal actor projection in one file. The behavior was correct, but the file had crossed the 800-line soft limit and put Radar/Registry/Atlas projection changes in the same owner as Tribunal actor naming.
- Generic Trigger Path: Maintain `greenfield create --confirm` artifact projections, project dashboard participant cards, or deterministic Tribunal visible actors after a confirmed Product Intent Confirmation is accepted. A small actor-naming or graph-normalization change should not touch the whole artifact projection module.
- Additional Root Cause: Graph extraction and visible actor projection both grew around artifact enrichment because they reused normalized backlog rows. Once those helpers fed the Tribunal and project-intelligence participants, their ownership became graph normalization and actor naming rather than artifact projection.
- Additional Invariant Violated: Artifact projection must not own the graph model or Tribunal actor naming. Graph normalization, visible actor selection, and artifact projection need separate owners so future generated-artifact quality fixes do not recreate an oversized artifact enrichment module.
- Required Guardrail: Keep `DomainIntelligenceGraph`, graph layer coercion, state-object selection, actor rows, and approval rows in `artifact_graph.py`. Keep Tribunal stable roles, domain actor naming, proposal actor candidate selection, actor label cleanup, and visible actor dedupe in `artifact_tribunal_actors.py`. `artifact_enrichment.py` may call those owners, but it must not export or re-own their helpers.
- Verification Added: `tests/unit/runtime/test_greenfield_proposals.py::test_artifact_enrichment_graph_and_tribunal_actors_stay_in_dedicated_owners` pins the owner boundary, parent line-count ceiling, and public export list. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/artifact_enrichment.py src/odylith/runtime/domain_intelligence/artifact_graph.py src/odylith/runtime/domain_intelligence/artifact_tribunal_actors.py src/odylith/runtime/domain_intelligence/proposal_tribunal.py src/odylith/runtime/project_intelligence/greenfield_participant_cards.py src/odylith/runtime/project_intelligence/greenfield_sources.py tests/unit/runtime/test_greenfield_proposals.py`; focused projection proof passed (`13 passed, 74 deselected in 13.48s`); standard greenfield artifact-quality bundle passed (`143 passed in 235.82s`); confirmed-create performance proof passed (`1 passed in 11.44s`).
- Agent Guardrails: For future artifact-enrichment work, route graph normalization through `artifact_graph.py` and visible Tribunal actor changes through `artifact_tribunal_actors.py`; do not put graph construction, state-object selection, actor candidate selection, or visible actor dedupe back into `artifact_enrichment.py`.

## 2026-06-03 Follow-up: Confirmed Components Needed Project Brief And Command Text Owners

- Fresh Finding: `greenfield_confirmed_components.py` still owned confirmed Registry component generation, project-brief rendering, host-independent path copy, and shell command quoting in one file. The behavior was correct, but the file sat just below the 800-line soft limit and mixed component artifact generation with project-readiness narration.
- Generic Trigger Path: Maintain `greenfield create --confirm` when accepted product intent needs both candidate Registry components and a project-first brief with coding-readiness gates and host-independent commands. A brief-copy or command-formatting change should not touch component row generation.
- Additional Root Cause: Project-brief rendering was introduced beside component generation because both run while constructing the confirmed proposal object. Once the brief became the project-first UX contract and shell commands were reused by rendering/review card paths, its ownership no longer matched the component generator.
- Additional Invariant Violated: Confirmed component generation must not own project-readiness copy or duplicated command quoting. Registry component row generation, project-brief rendering, and command text need separate owners so future artifact-quality fixes do not grow the component generator back toward the soft limit.
- Required Guardrail: Keep confirmed Registry component rows, component labels, component IDs, and component-local contract repair in `greenfield_confirmed_components.py`. Keep project brief sections, customization options, pre-coding checkpoints, coding-readiness gates, and host-independent paths in `greenfield_confirmed_project_brief.py`. Keep shell command quoting in `greenfield_command_text.py`, reused by proposal rendering, review cards, and confirmed proposal closeout text.
- Verification Added: `tests/unit/runtime/test_greenfield_component_spec_quality.py::test_confirmed_project_brief_stays_in_dedicated_owner` pins the project-brief owner and single command-text owner. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_command_text.py src/odylith/runtime/domain_intelligence/greenfield_confirmed_components.py src/odylith/runtime/domain_intelligence/greenfield_confirmed_project_brief.py src/odylith/runtime/domain_intelligence/greenfield_confirmed_proposal.py src/odylith/runtime/domain_intelligence/proposal_rendering.py src/odylith/runtime/domain_intelligence/proposal_review_card.py tests/unit/runtime/test_greenfield_component_spec_quality.py`; focused component/proposal proof passed (`60 passed in 21.26s`); standard greenfield artifact-quality bundle passed (`144 passed in 238.94s`); confirmed-create performance proof passed (`1 passed in 11.48s`).
- Agent Guardrails: For future confirmed-create component work, route project brief and host command copy through `greenfield_confirmed_project_brief.py` and `greenfield_command_text.py`; do not put project-readiness gates, host-independent path rows, or shell quoting back into `greenfield_confirmed_components.py`.

## 2026-06-03 Follow-up: Component Contracts Needed A Shared Phrase Owner

- Fresh Finding: Component contract generation still had local short-list and fallback term helpers split across `greenfield_component_contract.py`, `greenfield_component_semantic_contract.py`, and `greenfield_component_contract_differentiation.py`. The behavior was correct, but the repeated `_term_phrase`, `_phrase`, and `_content_terms` helpers made Registry contract text easier to drift during future confirmed-create quality fixes.
- Generic Trigger Path: Maintain `greenfield create --confirm` Registry component specs when generated component contracts need natural short lists, comma-clause lists, or fallback domain terms. A small wording fix should touch the component-term owner rather than each contract renderer.
- Additional Root Cause: The component-term module already owned artifact and domain term cleanup, but natural short-list rendering and fallback term filtering remained where each renderer first needed them.
- Additional Invariant Violated: Registry component contract text must use one shared term and phrase owner so the same generated-artifact clarity rules apply to base contracts, semantic contracts, and contract differentiation.
- Required Guardrail: Keep `natural_phrase`, comma-clause `phrase`, and `domain_terms` in `greenfield_component_terms.py`. Contract renderers may call those helpers, but they must not reintroduce local `_term_phrase`, `_phrase`, or `_content_terms` clones.
- Verification Added: `tests/unit/runtime/test_greenfield_component_spec_quality.py::test_component_contract_phrase_helpers_stay_in_terms_owner` pins the owner boundary, local-helper absence, and formatter behavior. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_component_terms.py src/odylith/runtime/domain_intelligence/greenfield_component_contract.py src/odylith/runtime/domain_intelligence/greenfield_component_semantic_contract.py src/odylith/runtime/domain_intelligence/greenfield_component_contract_differentiation.py tests/unit/runtime/test_greenfield_component_spec_quality.py`; focused component-spec proof passed (`12 passed in 0.23s`); standard greenfield artifact-quality bundle passed (`145 passed in 243.87s`); confirmed-create performance proof passed (`1 passed in 11.93s`).
- Agent Guardrails: For future Registry component contract wording work, route term filtering and list rendering through `greenfield_component_terms.py`; do not add private phrase or content-term helpers back to the contract renderers.

## 2026-06-03 Follow-up: Confirmed Completion Needed A Component Row Owner

- Fresh Finding: `greenfield_confirmed_completion.py` still owned the repair loop plus component contract normalization, contract-derived responsibility and boundary repair, interface/dependency/validation/risk defaults, component risk enrichment, and component sentence repair. The parent stayed just below the soft limit but mixed orchestration with Registry component row ownership.
- Generic Trigger Path: Maintain `greenfield create --confirm` when generated Registry component rows need repair before the proposal Tribunal and governed artifact Tribunals run. A component contract wording or risk-enrichment change should not touch the whole confirmed-create repair orchestrator.
- Additional Root Cause: Component row completion was added to the confirmed completion loop as a phase, then kept its local weakness checks and risk enrichment helpers in the parent after other confirmed-create phases moved to dedicated owners.
- Additional Invariant Violated: Confirmed-create repair orchestration must not own Registry component row completion details. Component contract normalization, defaults, risk enrichment, and sentence repair need a dedicated owner so Registry component artifact quality can improve without growing the parent.
- Required Guardrail: Keep `complete_component_rows` and `repair_component_sentence_lists` in `greenfield_confirmed_component_completion.py`. `greenfield_confirmed_completion.py` may call those functions, but it must not reintroduce component contract normalizers, contract-derived field repair, component weakness checks, or component risk-context scoring.
- Verification Added: `tests/unit/runtime/test_greenfield_confirmed_repair.py::test_confirmed_completion_prewrite_gate_stays_in_dedicated_owner` now pins the component-completion owner and parent line-count ceiling. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_confirmed_completion.py src/odylith/runtime/domain_intelligence/greenfield_confirmed_component_completion.py tests/unit/runtime/test_greenfield_confirmed_repair.py`; focused confirmed-repair proof passed (`3 passed in 10.34s`); focused component-risk/watch-path proof passed (`1 passed in 3.92s`); standard greenfield artifact-quality bundle passed (`145 passed in 234.45s`); confirmed-create performance proof passed (`1 passed in 11.48s`).
- Agent Guardrails: For future confirmed-create Registry component repair work, route component row completion and sentence repair through `greenfield_confirmed_component_completion.py`; do not add component contract field repair, risk enrichment, or weakness checks back into `greenfield_confirmed_completion.py`.

## 2026-06-03 Follow-up: Greenfield Row Coercion Needed A Shared Owner

- Fresh Finding: Generated proposal row coercion was still duplicated as small private `_mapping_rows`, `_dict_rows`, `_row_count`, and `_mapping_count` helpers in confirmed completion, confirmed prewrite gating, the deterministic Tribunal, confirmed title repair, apply-prewrite remapping, and post-confirm package reporting while post-confirm checks used a phase-specific `greenfield_post_confirm_rows.py` owner. The behavior was correct, but the ownership encouraged future row-shape fixes to fork across greenfield phases.
- Generic Trigger Path: Maintain `greenfield create --confirm` when proposal, semantic model, prewrite, Tribunal, or memory-preview rows contain mixed list values. A row-shape fix should change one helper, not several private copies.
- Additional Root Cause: Row coercion was introduced locally wherever a generated surface first needed `list[Mapping]` or mutable `list[dict]` filtering. The first shared helper was named for post-confirm completion, so other phases kept local copies instead of depending on a neutral owner.
- Additional Invariant Violated: Confirmed-create hot paths must not carry duplicate row coercion helpers. Proposal row filtering is cross-phase infrastructure and should not be owned by Tribunal, prewrite, title repair, or post-confirm package modules.
- Required Guardrail: Keep generated list-row coercion and package row counts in `greenfield_rows.py`. Confirmed completion, confirmed prewrite gates, Tribunal checks, title repair, apply-prewrite remapping, and post-confirm semantic/package checks may call `mapping_rows`, `dict_rows`, `row_count`, or `mapping_count`, but they must not reintroduce private `_mapping_rows`/`_dict_rows` helpers, package-local `_row_count`/`_mapping_count` helpers, or a phase-specific compatibility wrapper.
- Verification Added: `tests/unit/runtime/test_greenfield_row_coercion.py::test_greenfield_mapping_row_coercion_stays_in_shared_owner` pins `greenfield_rows.py`, verifies the old post-confirm-specific owner is absent, and rejects private row coercion/count helpers in the touched greenfield phases. Replayable proof: `.venv/bin/python -m py_compile src/odylith/runtime/domain_intelligence/greenfield_rows.py src/odylith/runtime/domain_intelligence/greenfield_post_confirm_completion.py src/odylith/runtime/domain_intelligence/greenfield_post_confirm_semantic_alignment.py src/odylith/runtime/domain_intelligence/greenfield_post_confirm_semantic_drift.py src/odylith/runtime/domain_intelligence/greenfield_confirmed_completion.py src/odylith/runtime/domain_intelligence/greenfield_confirmed_prewrite_gate.py src/odylith/runtime/domain_intelligence/proposal_tribunal.py src/odylith/runtime/domain_intelligence/greenfield_confirmed_title_repair.py src/odylith/runtime/domain_intelligence/greenfield_apply_prewrite.py tests/unit/runtime/test_greenfield_general_artifact_quality.py tests/unit/runtime/test_greenfield_row_coercion.py`; focused row/post-confirm proof passed (`3 passed, 38 deselected in 4.98s`); standard greenfield artifact-quality bundle passed (`146 passed in 235.23s`); confirmed-create performance proof passed (`1 passed in 11.63s`).
- Agent Guardrails: For future generated proposal row handling, route list-to-mapping filtering, list-to-dict filtering, and generated package row counts through `greenfield_rows.py`; do not add private row coercion or count helpers back into completion, prewrite, Tribunal, title repair, or post-confirm package files.

## 2026-06-03 Follow-up: Component Rows And Semantic Context Needed The Same Shared Owners

- Fresh Finding: After the first row-coercion pass, remaining greenfield readers still had package-local `_component_rows` and `_created_rows` helpers in backlog impact, component completion, contract differentiation, and implementation handoff code. The broader post-confirm suite also exposed generated-copy regressions where project-brief metadata and proof text could preserve `resident create repair request` as a Registry artifact phrase or `shows the resident a confirmation` as posture copy.
- Generic Trigger Path: Maintain `greenfield create --confirm` when Radar impact metadata, implementation handoff output, Registry component contract repair, or project-brief posture text reads generated proposal rows or accepted proof text. A row-shape or semantic-copy fix should land in the shared row owner or semantic context owner, not in every caller.
- Additional Root Cause: The initial shared row owner covered the obvious proposal/package phases, but component-facing and handoff-facing readers still treated list filtering as local glue. Semantic context cleanup also removed actor/action prefixes only at the first term, so generated metadata keys such as `must_capture`, `recommended`, and `operator_question` could shift the actor/action pair into artifact output.
- Additional Invariant Violated: Confirmed-create row filtering must have one owner across proposal, component, backlog, handoff, and package phases. Confirmed semantic context must strip metadata prefixes before actor/action cleanup, and human-visible project-brief copy must not preserve awkward show-actor-result grammar.
- Required Guardrail: Keep `mapping_rows`, `dict_rows`, `row_count`, and `mapping_count` in `greenfield_rows.py`. `greenfield_backlog_impact.py`, `greenfield_experience.py`, `greenfield_confirmed_component_completion.py`, and `greenfield_component_contract_differentiation.py` may call those helpers, but they must not reintroduce `_component_rows` or `_created_rows`. Keep metadata-led actor/action cleanup in `greenfield_component_semantic_context.py`, and keep project-brief show-actor-result repair in `greenfield_confirmed_project_brief.py`.
- Verification Added: `tests/unit/runtime/test_greenfield_row_coercion.py::test_greenfield_mapping_row_coercion_stays_in_shared_owner` now rejects private `_component_rows` and `_created_rows` helpers in the touched component and handoff readers. Existing post-confirm slop regressions pin `resident create repair request` and `shows the resident a confirmation` cleanup. Replayable proof: syntax proof passed for all touched modules; focused row/component artifact proof passed (`59 passed in 164.10s`); the two exposed slop regressions passed (`2 passed in 1.15s`); broad confirmed-greenfield proof passed (`160 passed in 239.60s`); confirmed-create performance proof passed (`1 passed in 11.51s`).
- Agent Guardrails: For future generated row or semantic-copy fixes, first check `greenfield_rows.py`, `greenfield_component_semantic_context.py`, and `greenfield_confirmed_project_brief.py`; do not add another local list-filter helper or final-string artifact scrub in a caller.

## 2026-06-03 Follow-up: Confirmed Intent Text Lists Needed A Shared Owner

- Fresh Finding: Confirmed-intent parser, completion, actor completion, system completion, and validation modules still carried local `_strings` helpers for the same list-field cleanup. Some helpers used `clean_confirmed_text`; others delegated to generic `text_values`, so accepted intent list cleanup could drift across parser, completion, and validation phases.
- Generic Trigger Path: Maintain `greenfield create --confirm` when accepted Product Intent Confirmation fields such as actors, systems, assumptions, ambiguities, non-goals, and success metrics arrive as Markdown strings or explicit lists. A list-cleanup change should affect every confirmed-intent phase consistently.
- Additional Root Cause: As confirmed-intent responsibilities split into parser, actor completion, system completion, and validation owners, each module kept the small local helper it needed at extraction time instead of reusing the confirmed text owner.
- Additional Invariant Violated: Confirmed-intent list text coercion is cross-phase confirmed-text infrastructure. Parser, completion, actor, system, and validation modules must not own duplicate `_strings` helpers, and accepted-intent list coercion must not silently flatten mapping-shaped values into rows.
- Required Guardrail: Keep `confirmed_text_values` in `greenfield_confirmed_text.py`. Confirmed-intent parser, completion, actor-completion, system-completion, and validation modules may call it, but they must not reintroduce local `_strings` helpers or diverging Markdown/list cleanup.
- Verification Added: `tests/unit/runtime/test_greenfield_confirmed_text.py::test_confirmed_intent_list_text_coercion_stays_in_text_owner` pins the shared owner, strict no-mapping-flattening behavior, and absence of local `_strings` helpers across the touched confirmed-intent modules. Replayable proof: syntax proof passed for all touched modules; focused confirmed-text/intent proof passed (`29 passed in 39.80s`); broad confirmed-greenfield proof passed (`160 passed in 240.87s`); confirmed-create performance proof passed (`1 passed in 11.52s`).
- Agent Guardrails: For future confirmed-intent list field cleanup, route scalar/list text coercion through `greenfield_confirmed_text.py`; do not add private `_strings`, `_list_values`, or mapping-flattening compatibility helpers to parser, completion, actor, system, or validation modules.

## 2026-06-03 Follow-up: Domain Intelligence Coercion Needed Existing Shared Owners

- Fresh Finding: After confirmed text and row coercion were consolidated, `greenfield_workstream_intelligence.py` still carried a private `_list_values` wrapper around shared text coercion, and `project_intelligence_binding.py` still carried a private `_mapping` helper even though runtime mapping coercion already had a shared owner.
- Generic Trigger Path: Maintain `greenfield create --confirm` when workstream Domain Intelligence layers or project-intelligence artifact bindings read generated proposal values. A text-list or mapping-shape fix should land in the shared text or runtime coercion owner, not in the individual artifact reader.
- Additional Root Cause: The remaining helpers were small enough to look harmless, so they survived earlier larger row and confirmed-text cleanup passes even though they represented the same duplicate-helper class.
- Additional Invariant Violated: Confirmed-create hot paths must not carry private list or mapping coercion helpers when a shared owner already exists. Workstream Domain Intelligence should use `greenfield_text.text_values`, and project-intelligence binding should use `runtime.common.value_coercion.mapping_copy`.
- Required Guardrail: Keep general greenfield text-list coercion in `greenfield_text.py` and runtime mapping-copy coercion in `runtime/common/value_coercion.py`. Do not reintroduce `_list_values` in `greenfield_workstream_intelligence.py` or `_mapping` in `project_intelligence_binding.py`.
- Verification Added: `tests/unit/runtime/test_greenfield_coercion_hygiene.py` pins both source boundaries. Replayable proof: focused coercion/domain-intelligence/project-binding proof passed (`49 passed in 16.10s`); broader greenfield artifact-quality and confirmed-create proof passed (`119 passed in 233.71s`), including the under-30s confirmed-create performance gate.
- Agent Guardrails: For future B-142 coercion cleanup, route scalar/list text through `greenfield_text.py`, confirmed-intent list rows through `greenfield_confirmed_text.py`, generated row filtering through `greenfield_rows.py`, and runtime mapping copies through `runtime/common/value_coercion.py` before adding a private helper.

## 2026-06-03 Follow-up: Proposal Project Brief Rendering Needed Its Owning Module

- Fresh Finding: `proposal_rendering.py` still owned the rendered `project_brief` helper stack even after confirmed project-brief creation and generated row coercion had dedicated owners. The behavior was correct, but proposal text changes could still grow the general proposal renderer and bypass the shared generated-row contract.
- Generic Trigger Path: Maintain `greenfield propose`, `greenfield create --confirm`, or proposal review text when the project brief contains blueprint sections, customization options, pre-coding checkpoints, coding-readiness gates, or host-independent path commands.
- Additional Root Cause: The project brief was originally rendered inside proposal text because it appears near the top of the human proposal. Once the brief became a cross-host project-first UX contract, its rendering responsibility no longer matched the general proposal renderer.
- Additional Invariant Violated: Project-brief rendering must not be owned by the generic proposal text renderer. Brief rows should share the same generated-row coercion owner as other greenfield phases so future row-shape fixes do not fork.
- Required Guardrail: Keep `render_project_brief_lines`, blueprint-section rendering, option rendering, checkpoint rendering, and host-path rendering in `greenfield_project_brief.py`. `proposal_rendering.py` may call `render_project_brief_lines`, but it must not reintroduce `_project_brief_lines` or private project-brief rendering helpers.
- Verification Added: `tests/unit/runtime/test_greenfield_project_brief_rendering.py` pins the owner boundary and verifies mapping-shaped brief rows are rendered through shared `mapping_rows`. Replayable proof: syntax proof passed for the touched renderer modules and test; focused project-brief/proposal proof passed (`4 passed in 0.22s`); broader proposal and artifact-quality proof passed (`87 passed in 189.11s`); confirmed-create performance proof passed (`1 passed in 11.97s`).
- Agent Guardrails: For future proposal text work, route top-level project-brief formatting through `greenfield_project_brief.py` and generated brief row filtering through `greenfield_rows.py`; do not add private project-brief row renderers back into `proposal_rendering.py`.

## 2026-06-03 Follow-up: Component Axes Still Forked Component Term Helpers

- Fresh Finding: After component contract renderers moved to `greenfield_component_terms.py`, `greenfield_component_axes.py` still carried its own `_content_terms`, `_term_token`, `_phrase`, and `_normalize_axis_text` helpers. Derived semantic axes fed Registry component contracts from a local term fork while neighboring contract renderers used the shared owner.
- Generic Trigger Path: Maintain `greenfield create --confirm` when generated Registry components need derived semantic axes, component-local state, accepted inputs, produced outputs, proof obligations, or axis keys from arbitrary product vocabulary.
- Additional Root Cause: Component axes started as a small fallback model and kept local token and phrase helpers because they were short. Once axis keys and contract fields became part of the generated Registry quality path, those helpers became another drift point beside the shared component-term owner.
- Additional Invariant Violated: Registry component axis derivation must use the same component-term owner as component contract rendering. Axis-key and local-contract wording fixes should not require a separate tokenization or phrase helper in `greenfield_component_axes.py`.
- Required Guardrail: Keep component term extraction and term-phrase rendering in `greenfield_component_terms.py`. `greenfield_component_axes.py` may call `domain_terms` and `term_phrase`, but it must not reintroduce `_content_terms`, `_term_token`, `_phrase`, or `_normalize_axis_text`.
- Verification Added: `tests/unit/runtime/test_greenfield_component_spec_quality.py::test_component_contract_phrase_helpers_stay_in_terms_owner` now pins `greenfield_component_axes.py` to the shared term owner and verifies `term_phrase` behavior. Replayable proof: syntax proof passed for the touched modules and test; focused component-axis/spec proof passed (`15 passed in 13.16s`); broader component and artifact-quality proof passed (`54 passed in 166.71s`); confirmed-create performance proof passed (`1 passed in 11.73s`).
- Agent Guardrails: For future generated Registry axis or contract wording work, route domain-term extraction through `greenfield_component_terms.domain_terms` and space-joined term wording through `greenfield_component_terms.term_phrase`; do not add local tokenization, phrase, or axis-text normalizer helpers back into `greenfield_component_axes.py`.

## 2026-06-04 Follow-up: Component Contract Fields Still Forked Phrase Rendering

- Fresh Finding: After base contracts, semantic contracts, contract differentiation, and component axes moved to `greenfield_component_terms.py`, `greenfield_component_contract_fields.py` still carried a local `_phrase` helper for support-artifact text. The helper was tiny, but it left one more Registry contract-field path outside the shared component-term owner.
- Generic Trigger Path: Maintain `greenfield create --confirm` when generated Registry component fields need accepted-input or produced-output wording, especially adjustment support artifacts derived from arbitrary product vocabulary.
- Additional Root Cause: Contract-field support text looked like a narrow local list join, so it survived the larger shared-term cleanup even though the same comma-clause behavior was already owned by `greenfield_component_terms.phrase`.
- Additional Invariant Violated: Registry component contract fields must use the same component-term phrase owner as base contracts, semantic contracts, contract differentiation, and component axes. Field-level support-artifact wording fixes should not require a separate `_phrase` helper in `greenfield_component_contract_fields.py`.
- Required Guardrail: Keep comma-clause phrase rendering in `greenfield_component_terms.phrase`. `greenfield_component_contract_fields.py` may call that helper, but it must not reintroduce a local `_phrase` clone for support artifacts or contract-list text.
- Verification Added: `tests/unit/runtime/test_greenfield_component_spec_quality.py::test_component_contract_phrase_helpers_stay_in_terms_owner` now pins `greenfield_component_contract_fields.py` to the shared phrase owner and verifies the local helper is absent. Replayable proof: syntax proof passed for the touched modules and test; focused component-spec proof passed (`12 passed in 0.24s`); broader component and artifact-quality proof passed (`54 passed in 170.39s`); confirmed-create performance proof passed (`1 passed in 13.04s`).
- Agent Guardrails: For future generated Registry contract-field wording work, route comma-clause rendering through `greenfield_component_terms.phrase`; do not add local phrase helpers back into `greenfield_component_contract_fields.py`.

## 2026-06-04 Follow-up: Component Quality Gate Still Owned Term Indexing

- Fresh Finding: After component terms became the shared owner for contract wording, `greenfield_component_contract_quality.py` still defined `ordered_domain_terms` and a local `_term_token` cache. Component terms, component contracts, and contract differentiation imported term extraction from the quality gate, so a quality-check module still owned reusable Registry term indexing.
- Generic Trigger Path: Maintain `greenfield create --confirm` when generated Registry components need component-local term matching, contract differentiation, component-local proof checks, or rendered-spec quality gates across arbitrary product vocabulary.
- Additional Root Cause: Ordered term extraction started inside the quality gate because the first failures were rendered-spec overlap checks. Once the same extraction fed component terms and contract rendering, quality-gate ownership became a hidden dependency rather than the right abstraction.
- Additional Invariant Violated: Registry quality gates may consume component-local term indexing, but they must not own it. Reusable term extraction must live in a dedicated term-index owner so contract rendering and quality checks cannot drift through a quality-gate backdoor.
- Required Guardrail: Keep ordered component-local term indexing in `greenfield_component_term_index.py`. Component contracts, component terms, contract differentiation, and quality gates may call `ordered_domain_terms`, but `greenfield_component_contract_quality.py` must not reintroduce local `ordered_domain_terms` or `_term_token` helpers.
- Verification Added: `tests/unit/runtime/test_greenfield_component_spec_quality.py::test_component_contract_phrase_helpers_stay_in_terms_owner` now pins the term-index owner, requires `greenfield_component_contract_quality.py` not to define `ordered_domain_terms` or `_term_token`, and verifies the extracted term behavior. Replayable proof: syntax proof passed for the touched modules and test; focused component-spec proof passed (`12 passed in 0.22s`); broader component and artifact-quality proof passed (`54 passed in 152.75s`); confirmed-create performance proof passed (`1 passed in 9.95s`).
- Agent Guardrails: For future generated Registry term-quality work, import ordered component-local terms from `greenfield_component_term_index.py`; do not add reusable term indexing back into quality gates or individual renderers.

## 2026-06-04 Follow-up: Product Risks Still Forked Domain Term Specificity

- Fresh Finding: After component term indexing moved out of the quality gate, `greenfield_product_risks.py` still carried a local `_domain_terms` helper for Radar-facing product risk specificity. Product-risk genericity checks and Registry component term matching were normalizing product vocabulary through separate token paths.
- Generic Trigger Path: Maintain `greenfield create --confirm` when generated Radar workstreams and project dashboards need product-facing risks that are specific enough to the accepted intent and not generic greenfield boilerplate.
- Additional Root Cause: Product risks originally needed only a small overlap check, so the helper stayed local. Once Radar product-risk specificity became part of the same anti-slop quality lane as Registry component term matching, the local helper became a fork of the shared term-index behavior.
- Additional Invariant Violated: Radar product-risk specificity and Registry component term matching must share one host/model/project-agnostic token normalization kernel while keeping their own caller-owned stopwords. Product-risk checks should not carry a private `_domain_terms` implementation.
- Required Guardrail: Keep reusable token normalization in `greenfield_domain_term_index.ordered_terms`. Product risks may pass risk-specific stopwords, and component term indexing may pass component-specific stopwords, but neither path should reintroduce local regex/token loops for artifact specificity.
- Verification Added: `tests/unit/runtime/test_greenfield_proposals.py::test_product_risk_specificity_uses_shared_term_index` pins the shared term-index owner, product-risk caller, component-term delegation, and absence of the local `_domain_terms` helper. Replayable proof: syntax proof passed for the touched modules and test; focused proposal proof passed (`46 passed in 15.79s`); focused component proof passed (`12 passed in 0.23s`); broader artifact/proposal proof passed (`100 passed in 176.18s`); confirmed-create performance proof passed (`1 passed in 11.47s`).
- Agent Guardrails: For future Radar risk or Registry specificity work, route reusable term normalization through `greenfield_domain_term_index.ordered_terms` and pass surface-specific stopwords instead of adding private `_domain_terms` or token loops.

## 2026-06-04 Follow-up: Component Quality Still Owned Spec Term Distinctiveness

- Fresh Finding: After ordered component terms moved into `greenfield_component_term_index.py`, `greenfield_component_contract_quality.py` still owned `domain_terms`, `_section_terms`, and `_local_domain_terms`. The quality gate was still responsible for Registry spec term sets, section-term filtering, and sibling distinctiveness scoring instead of only deciding whether the generated specs passed.
- Generic Trigger Path: Maintain `greenfield create --confirm` when generated Registry specs must prove they are component-local, not interchangeable, and not repeated section boilerplate across arbitrary product vocabularies.
- Additional Root Cause: Rendered-spec quality checks first needed the distinctiveness scoring, so the scoring stayed inside the quality gate after ordered term extraction was split. Once term indexing became a shared owner, leaving local term scoring in the gate kept a partial shared-kernel adoption.
- Additional Invariant Violated: Registry quality gates may decide failure messages, but reusable component-domain term sets, section term filtering, and local distinctiveness scoring must live in the component term-index owner. Quality gates should not export or define term-index helpers.
- Required Guardrail: Keep `component_domain_terms`, `section_domain_terms`, and `component_local_terms` in `greenfield_component_term_index.py`. `greenfield_component_contract_quality.py` may call those helpers, but it must not reintroduce `domain_terms`, `_section_terms`, `_local_domain_terms`, or direct stopword ownership for Registry spec distinctiveness.
- Verification Added: `tests/unit/runtime/test_greenfield_component_spec_quality.py::test_component_contract_phrase_helpers_stay_in_terms_owner` now pins the term-index owner for ordered terms, component-domain term sets, section terms, and local distinctiveness scoring while requiring the quality gate not to define those helpers. Replayable proof: syntax proof passed for the touched modules and test; focused component-spec proof passed (`12 passed in 0.24s`); broader artifact/proposal proof passed (`128 passed in 222.27s`); confirmed-create performance proof passed (`1 passed in 11.39s`).
- Agent Guardrails: For future Registry spec quality work, keep term-set and distinctiveness behavior in `greenfield_component_term_index.py`; add quality-gate failures around that owner instead of adding local term scoring back into `greenfield_component_contract_quality.py`.

## 2026-06-04 Follow-up: Confirmed Intent Still Forked Semantic Term Extraction

- Fresh Finding: `greenfield_confirmed_text.py` already exposed `semantic_terms`, but `greenfield_confirmed_intent_validation.py` and `greenfield_confirmed_system_rows.py` still carried local `_semantic_terms` loops and local stopword ownership. Confirmed Product Intent validation and internal-system row parsing were normalizing accepted narrative vocabulary through separate paths.
- Generic Trigger Path: Maintain `greenfield create --confirm` when a confirmed Product Intent Confirmation needs qualitative story/path/proof overlap and internal product-system row parsing across arbitrary domains.
- Additional Root Cause: The first validation split moved `_semantic_terms` out of the parser into validation, but did not finish the migration to the confirmed-text owner that later became shared by actor and system completion. System row parsing kept its own smaller loop for contextual clause matching, leaving two local forks beside the owner.
- Additional Invariant Violated: Confirmed-intent semantic term extraction must have one text owner. Validation may choose stricter stopwords, but it should pass them to `greenfield_confirmed_text.semantic_terms`; internal-system row parsing should import that owner instead of calling `normalize_domain_token` directly.
- Required Guardrail: Keep `semantic_terms` and confirmed-intent validation stopwords in `greenfield_confirmed_text.py`. `greenfield_confirmed_intent_validation.py` and `greenfield_confirmed_system_rows.py` must not reintroduce `_TERM_STOPWORDS`, `_semantic_terms`, or direct `normalize_domain_token` loops for accepted-intent semantic overlap.
- Verification Added: `tests/unit/runtime/test_greenfield_confirmed_text.py::test_confirmed_intent_semantic_terms_stay_in_text_owner` pins the owner, validation caller, system-row caller, stopword handoff, and absence of local token loops. The oversized confirmed-intent test module was split into `tests/unit/runtime/test_greenfield_confirmed_intent_ownership.py`, keeping `test_greenfield_confirmed_intent.py` below the 1500-line test ceiling. Replayable proof: syntax proof passed for the touched modules and tests; focused confirmed-intent proof passed (`30 passed in 37.40s`); broader artifact/proposal proof passed (`130 passed in 204.69s`); confirmed-create performance proof passed (`1 passed in 9.91s`).
- Agent Guardrails: For future accepted-intent validation, parser, actor, or internal-system work, route semantic token extraction through `greenfield_confirmed_text.semantic_terms` and pass caller-owned stopwords instead of adding local token loops.

## 2026-06-04 Follow-up: Sequence Diagram Routing Still Forked Domain Term Indexing

- Fresh Finding: `greenfield_sequence_diagram.py` still carried a private `_domain_terms` token loop for first-path sequence and flowchart component matching even after Radar risk specificity and Registry component matching moved to `greenfield_domain_term_index.ordered_terms`.
- Generic Trigger Path: Maintain `greenfield create --confirm` when generated Atlas first-path sequence or flowchart diagrams must route arbitrary accepted path steps to the best active component.
- Additional Root Cause: Sequence routing needed caller-owned stopwords and a small gerund-collapse behavior, so its local helper survived the first shared domain-term index pass instead of extending the shared owner with that option.
- Additional Invariant Violated: Generated-artifact term extraction must not fork regex/token loops per artifact. Atlas sequence routing may own sequence-specific stopwords, but reusable token extraction and normalization belong in `greenfield_domain_term_index.py`.
- Required Guardrail: Keep domain-term extraction in `greenfield_domain_term_index.ordered_terms`. `greenfield_sequence_diagram.py` may call it with sequence stopwords and `stem_ing=True`, but it must not reintroduce `_domain_terms`, `_term_token`, or direct `normalize_domain_token` loops for component routing.
- Verification Added: `tests/unit/runtime/test_greenfield_confirmed_diagrams.py::test_sequence_diagram_term_routing_uses_shared_index` pins the shared owner, `stem_ing` behavior, sequence caller, and absence of local token loops. Replayable proof: syntax proof passed for the touched modules and test; focused diagram proof passed (`5 passed in 0.05s`); wider artifact/proposal proof passed (`105 passed in 166.48s`); confirmed-create performance proof passed (`1 passed in 9.90s`).
- Agent Guardrails: For future Atlas sequence, flowchart, or artifact-specific matching work, add caller options to the shared term index when needed instead of cloning token loops inside renderers.

## 2026-06-04 Follow-up: Semantic Model Still Forked Domain Term Indexing

- Fresh Finding: `greenfield_semantic_model.py` still carried a private `_semantic_terms` token loop and direct `normalize_domain_token` import for ontology terms, required fields, event targets, and actor terms after product-risk specificity and Atlas sequence routing had moved to `greenfield_domain_term_index.ordered_terms`.
- Generic Trigger Path: Maintain `greenfield create --confirm` when the generated semantic model drives Radar workstream language, Registry component references, Atlas diagram event graphs, and Tribunal proof checks across arbitrary accepted product narratives.
- Additional Root Cause: The semantic model needed its own stopword policy and was introduced as a typed model owner, so term extraction stayed local even after the reusable ordered-term owner existed.
- Additional Invariant Violated: Generated semantic model term extraction must share the greenfield domain term index. The semantic-model owner may retain semantic-model stopwords, but it must not own a separate regex/token loop or direct token normalization.
- Required Guardrail: Keep reusable domain-term extraction in `greenfield_domain_term_index.ordered_terms`. `greenfield_semantic_model.py` may pass `_SEMANTIC_MODEL_TERM_STOPWORDS`, but it must not reintroduce `_semantic_terms`, `_term_token`, or direct `normalize_domain_token` loops for generated semantic model vocabulary.
- Verification Added: `tests/unit/runtime/test_greenfield_intelligence_schema.py::test_semantic_model_term_extraction_uses_shared_domain_index` pins the shared owner, semantic-model caller, source boundary, ontology terms, and required-field behavior. Replayable proof: syntax proof passed for the touched module and test; focused intelligence-schema proof passed (`3 passed in 0.11s`); wider artifact/proposal/post-confirm proof passed (`108 passed in 168.60s`); confirmed-create performance proof passed (`1 passed in 9.91s`).
- Agent Guardrails: For future semantic-model, generated-artifact, or Tribunal vocabulary work, pass caller-owned stopwords into `greenfield_domain_term_index.ordered_terms` instead of adding local token loops to model or gate modules.

## 2026-06-04 Follow-up: Post-Confirm Drift Still Forked Domain Term Indexing

- Fresh Finding: `greenfield_post_confirm_semantic_drift.py` still imported `normalize_domain_token` directly and carried local regex token loops for semantic repetition signatures, semantic overlap scoring, and contrastive drift term signatures after the shared greenfield domain term index already owned reusable artifact vocabulary normalization.
- Generic Trigger Path: Maintain `greenfield create --confirm` when post-confirm package checks compare accepted intent, semantic model context, generated Radar/Registry/Atlas text, and prewrite preview copy across arbitrary product narratives.
- Additional Root Cause: Post-confirm drift checks were split into a dedicated owner before the shared term index was adopted across generated artifacts, so the module kept its local token loops as phase-specific glue.
- Additional Invariant Violated: Generated-artifact semantic drift, repetition, and overlap checks must share the same domain term normalization owner as product risks, Atlas sequence routing, and semantic-model vocabulary. The drift checker may keep post-confirm stopword policy and separator cleanup, but it must not own reusable token extraction.
- Required Guardrail: Keep reusable domain-term extraction in `greenfield_domain_term_index.ordered_terms`. `greenfield_post_confirm_semantic_drift.py` may pass post-confirm stopwords and split hyphen/underscore separators for overlap scoring, but it must not reintroduce direct `normalize_domain_token` imports or local regex token loops.
- Verification Added: `tests/unit/runtime/test_greenfield_semantic_drift_terms.py::test_post_confirm_semantic_drift_terms_use_shared_index` pins the shared owner, post-confirm caller, absence of local token loops, and an overlap behavior example. Replayable proof: syntax proof passed for the touched module and test; focused drift proof passed (`2 passed in 0.11s`); wider artifact/proposal/post-confirm proof passed (`101 passed in 164.28s`); confirmed-create performance proof passed (`1 passed in 9.69s`).
- Agent Guardrails: For future post-confirm semantic drift, repetition, overlap, or generated-artifact vocabulary checks, pass caller-owned stopwords into `greenfield_domain_term_index.ordered_terms` instead of adding local token loops to drift or package modules.

## 2026-06-04 Follow-up: Confirmed Artifact Tribunal Still Forked Domain Term Indexing

- Fresh Finding: `proposal_tribunal_substance.py` still imported `normalize_domain_token` directly and carried local regex token loops for confirmed Radar substance terms, Registry proof-boundary terms, and Atlas first-path tail terms after the shared greenfield domain term index already owned reusable generated-artifact vocabulary normalization.
- Generic Trigger Path: Maintain the confirmed greenfield Tribunal when generated Radar workstreams, Registry component proofs, and Atlas first-path diagrams must prove project-specific substance before source truth writes.
- Additional Root Cause: The confirmed artifact substance gate was split into its own Tribunal owner before the shared term index was adopted across generated-artifact checks, so its local `_term_set` survived as phase-specific glue.
- Additional Invariant Violated: Confirmed artifact Tribunal substance checks must share the same domain term normalization owner as product risks, Atlas routing, semantic-model vocabulary, and post-confirm drift. The Tribunal owner may keep Tribunal-specific stopwords and Atlas action aliases, but it must not own reusable token extraction.
- Required Guardrail: Keep reusable domain-term extraction in `greenfield_domain_term_index.ordered_terms`. `proposal_tribunal_substance.py` may pass Tribunal stopwords and preserve Atlas action aliases for short verb roots, but it must not reintroduce direct `normalize_domain_token` imports or local regex token loops.
- Verification Added: `tests/unit/runtime/test_greenfield_tribunal_term_index.py::test_confirmed_artifact_tribunal_terms_use_shared_index` pins the shared owner, Tribunal caller, absence of local token loops, and short Atlas action-root alias behavior. Replayable proof: syntax proof passed for the touched module and test; focused Tribunal proof passed (`4 passed in 6.89s`); wider artifact/proposal/post-confirm proof passed (`102 passed in 162.96s`); confirmed-create performance proof passed (`1 passed in 9.68s`).
- Agent Guardrails: For future confirmed artifact Tribunal, generated-artifact substance, or Atlas tail vocabulary checks, pass caller-owned stopwords into `greenfield_domain_term_index.ordered_terms` instead of adding local token loops to Tribunal modules.

## 2026-06-04 Follow-up: Semantic Quality Still Forked Release-Scope Term Indexing

- Fresh Finding: `greenfield_semantic_quality.py` still carried a private `_terms` regex token loop, direct `normalize_domain_token` import, and local reminder/sharing folding after the shared greenfield domain term index already owned reusable generated-artifact vocabulary normalization.
- Generic Trigger Path: Maintain `greenfield create --confirm` when release-scope classification decides whether reminder, sharing, integration, reporting, or lifecycle components are first-path, supporting, deferred, or out of scope across arbitrary product narratives.
- Additional Root Cause: Release-scope classification needed semantic-quality stopwords and prefix folding for reminder and sharing vocabulary, so it kept a local helper instead of extending the shared term index with caller-owned alias options.
- Additional Invariant Violated: Semantic-quality release-scope and scope-context checks must share the same domain term normalization owner as product risks, Atlas routing, semantic-model vocabulary, post-confirm drift, and confirmed artifact Tribunal checks. Semantic quality may keep release-scope stopwords and alias policy, but it must not own reusable token extraction.
- Required Guardrail: Keep reusable domain-term extraction in `greenfield_domain_term_index.ordered_terms`. `greenfield_semantic_quality.py` may pass semantic-quality stopwords and caller-owned prefix aliases, but it must not reintroduce direct `normalize_domain_token` imports or local regex token loops.
- Verification Added: `tests/unit/runtime/test_greenfield_semantic_quality_terms.py::test_semantic_quality_terms_use_shared_index_aliases` pins the shared owner, prefix-alias behavior, semantic-quality caller, absence of local token loops, and deferred reminder/sharing release-scope behavior. Replayable proof: syntax proof passed for the touched modules and test; focused semantic-quality proof passed (`1 passed in 0.03s`) and the health-tracking release-scope scenario passed (`1 passed in 3.59s`); wider artifact/proposal/post-confirm proof passed (`111 passed in 162.71s`); confirmed-create performance proof passed (`1 passed in 9.67s`).
- Agent Guardrails: For future semantic-quality release-scope, scope-context, or generated-artifact vocabulary work, pass caller-owned stopwords and aliases into `greenfield_domain_term_index.ordered_terms` instead of adding local token loops to semantic-quality modules.

## 2026-06-04 Follow-up: Confirmed Text Still Owned Reusable Term Indexing

- Fresh Finding: `greenfield_confirmed_text.py` had become the accepted-intent semantic owner, but its `semantic_terms` API still imported `normalize_domain_token` directly and carried a local regex token loop after generated-artifact term normalization moved to `greenfield_domain_term_index.ordered_terms`.
- Generic Trigger Path: Maintain `greenfield create --confirm` when confirmed Product Intent validation, actor completion, internal-system completion, and internal-system row parsing compare accepted story, first path, proof boundary, actor labels, and system clauses across arbitrary domains.
- Additional Root Cause: The prior cleanup correctly moved semantic term extraction out of parser, validation, and system-row callers, but stopped at the confirmed-text owner. That left a partial shared-kernel adoption: callers were unified, while token indexing still forked below the public API.
- Additional Invariant Violated: Confirmed-intent semantic terms must keep a single accepted-intent API in `greenfield_confirmed_text.semantic_terms`, but reusable token indexing and normalization should still go through the shared greenfield domain term index. Confirmed text may own confirmed Markdown cleanup, default stopwords, and caller handoff, but it must not own another regex token loop.
- Required Guardrail: Keep accepted-intent semantic term access in `greenfield_confirmed_text.semantic_terms`, implemented through `greenfield_domain_term_index.ordered_terms` with caller-owned stopwords and `stem_ing_minimum_length` when needed. `greenfield_confirmed_text.py` must not reintroduce direct `normalize_domain_token` imports or a local `for raw in re.findall` semantic-token loop.
- Verification Added: `tests/unit/runtime/test_greenfield_confirmed_text.py::test_confirmed_intent_semantic_terms_stay_in_text_owner` now pins the shared term-index delegation, confirmed-specific gerund threshold behavior, absence of local token loops, validation caller, and system-row caller. Replayable proof: syntax proof passed for the touched modules and test; focused confirmed-text proof passed (`2 passed in 0.03s`); focused confirmed-intent proof passed (`30 passed in 35.33s`); wider artifact/proposal/post-confirm proof passed (`141 passed in 200.44s`); confirmed-create performance proof passed (`1 passed in 9.63s`).
- Agent Guardrails: For future accepted-intent validation, actor, system, or parser work, keep the public `semantic_terms` API in confirmed text, but route reusable token extraction through `greenfield_domain_term_index.ordered_terms` instead of adding local token loops to confirmed-text or caller modules.

## 2026-06-04 Follow-up: Registry Component Terms Still Forked Token Indexing

- Fresh Finding: `greenfield_component_terms.py` still imported `normalize_domain_token` directly for phrase identity terms, and `greenfield_component_contract_fields.py` still carried a private transition-candidate regex token loop. `greenfield_component_semantic_contract.py` also kept a pass-through `_phrase_identity_terms` wrapper after the component-term owner already existed.
- Generic Trigger Path: Maintain `greenfield create --confirm` when generated Registry component specs need phrase identity, deduped object phrases, accepted-input or produced-output support artifacts, and state-transition text across arbitrary product vocabulary.
- Additional Root Cause: The earlier Registry term-index cleanup moved quality and component-domain term sets first, but phrase-identity and transition-candidate helpers looked small enough to remain local. That left a partial shared-kernel adoption inside the generated Registry component path.
- Additional Invariant Violated: Registry component term helpers may own component policy, but reusable token extraction belongs in `greenfield_domain_term_index.ordered_terms`. Semantic contract assembly should call the component phrase-identity owner directly, not hide it behind a local wrapper.
- Required Guardrail: Keep component phrase identity policy in `greenfield_component_terms.phrase_identity_terms` and transition-candidate token extraction in `greenfield_domain_term_index.ordered_terms`. `greenfield_component_terms.py` and `greenfield_component_contract_fields.py` must not import `normalize_domain_token` or reintroduce local `for raw in re.findall` token loops for those term sets; `greenfield_component_semantic_contract.py` must not reintroduce a pass-through `_phrase_identity_terms` wrapper.
- Verification Added: `tests/unit/runtime/test_greenfield_component_spec_quality.py::test_component_contract_phrase_helpers_stay_in_terms_owner` now pins the shared term-index imports, absence of local token loops, direct semantic-contract import, artifact-carrier phrase identity behavior, and transition text behavior. Replayable proof: syntax proof passed for the touched modules and test; focused component-spec proof passed (`12 passed in 0.21s`); wider artifact/proposal proof passed (`120 passed in 162.97s`); confirmed-create performance proof passed (`1 passed in 9.72s`).
- Agent Guardrails: For future generated Registry component wording, dedupe, state, or transition work, route reusable token extraction through `greenfield_domain_term_index.ordered_terms` and keep only caller-specific stopwords or state decisions in the component helper.

## 2026-06-04 Follow-up: Public Quality Gate Still Forked Prompt Term Indexing

- Fresh Finding: `greenfield_quality_gate.py` still imported `normalize_domain_token` directly and carried a local `_meaningful_terms` regex token loop for prompt echo and semantic-contract noun checks after generated-artifact term normalization had moved to `greenfield_domain_term_index.ordered_terms`.
- Generic Trigger Path: Maintain `greenfield create --confirm` when public Radar, Registry, Atlas, proposal, and project-dashboard text must reject prompt echoes, control-plane leakage, thin semantic contracts, and generic artifact copy across arbitrary domains.
- Additional Root Cause: The quality gate needed to preserve short domain abbreviations such as AI, ML, UI, and UX, so it kept a local exception path instead of extending the shared term index with a caller-owned short-term preservation option.
- Additional Invariant Violated: Public generated-artifact quality checks may own stopwords and failure decisions, but reusable token extraction and abbreviation preservation must stay in the shared greenfield domain term index. The quality gate must not own another regex/token loop.
- Required Guardrail: Keep prompt and semantic-contract term indexing in `greenfield_domain_term_index.ordered_terms` with caller-owned `preserve_terms` for short domain abbreviations. `greenfield_quality_gate.py` must not reintroduce direct `normalize_domain_token` imports, `_singular` token wrappers, or local `for raw in re.findall` loops for public quality terms.
- Verification Added: `tests/unit/runtime/test_greenfield_domain_profile_quality.py::test_quality_gate_meaningful_terms_use_shared_domain_index` pins the shared owner, absence of local token loops, short abbreviation preservation, and plural normalization behavior. Replayable proof: syntax proof passed for the touched modules and test; focused quality-gate proof passed (`17 passed in 13.90s`); wider greenfield proof passed (`168 passed in 226.89s`); confirmed-create performance proof passed (`1 passed in 9.44s`).
- Agent Guardrails: For future public quality-gate, prompt-echo, control-plane leak, or generated-artifact semantic contract checks, extend `greenfield_domain_term_index.ordered_terms` with caller-owned options instead of adding local token loops or singularization wrappers to the quality gate.

## 2026-06-04 Follow-up: First-Path Clauses Still Forked Actor Term Indexing

- Fresh Finding: `greenfield_first_path_clauses.py` still imported `normalize_domain_token` directly and carried a local regex token loop for actor signatures after first-path sequence routing, semantic-model vocabulary, Tribunal substance checks, and public quality gates had moved to `greenfield_domain_term_index.ordered_terms`.
- Generic Trigger Path: Maintain `greenfield create --confirm` when generated first-path action and capability clauses filter later actors after a visible result across arbitrary product narratives.
- Additional Root Cause: First-path clause rendering correctly owned grammar, action selection, and visible-result cleanup, but actor signature extraction looked small enough to remain local. That left a reusable token-indexing fork inside the path that decides whether later actor actions bleed into the first implementation slice.
- Additional Invariant Violated: First-path grammar may stay in the clause owner, but reusable actor-term extraction must share the same greenfield domain term index as other generated-artifact specificity and overlap checks. The clause owner may keep actor-specific stopwords and short actor-term preservation, but it must not own a direct token-normalizer loop.
- Required Guardrail: Keep actor signature term extraction in `greenfield_domain_term_index.ordered_terms` with caller-owned first-path stopwords and `preserve_terms` for short actor qualifiers such as AI, ML, UI, and UX. `greenfield_first_path_clauses.py` must not reintroduce direct `normalize_domain_token` imports or a local regex token loop for actor signatures.
- Verification Added: `tests/unit/runtime/test_greenfield_post_confirm_slop_regressions.py::test_first_path_clause_rendering_stays_in_dedicated_owner` now pins the shared owner and absence of direct token normalization in the clause renderer. `test_first_path_clauses_compile_actions_outcomes_and_noun_lists` now verifies a qualified actor such as `AI reviewer` does not collapse into a later plain `reviewer` action after a visible result. Replayable proof: syntax proof passed for the touched module and test; focused post-confirm slop proof passed (`14 passed in 2.78s`); wider greenfield proof passed (`151 passed in 237.66s`); confirmed-create performance proof passed (`1 passed in 11.47s`).
- Agent Guardrails: For future first-path action, actor, capability, or visible-result fixes, keep grammar in `greenfield_first_path_clauses.py`, but route reusable term extraction through `greenfield_domain_term_index.ordered_terms` instead of adding private regex token loops to first-path renderers.

## 2026-06-04 Follow-up: Confirmed Components Still Forked Domain Label Tokenization

- Fresh Finding: `greenfield_confirmed_components.py` still carried a local regex token loop inside `domain_label` after generated-artifact term normalization had moved to `greenfield_domain_term_index.py`.
- Generic Trigger Path: Maintain `greenfield create --confirm` when confirmed Product Intent titles or prompts derive the generated project/component label used by Radar, Registry, Atlas, runtime JSON, and dashboard copy.
- Additional Root Cause: Domain labels are visible titles, so they need to preserve casing, acronyms, and alphanumeric tokens rather than semantic singularization. That difference let the local extractor survive the earlier `ordered_terms` migration even though token extraction itself still belonged to the shared term-index owner.
- Additional Invariant Violated: Display-label token extraction and semantic term normalization are different policies, but both must share one tokenization owner. The confirmed component generator may own component naming policy and stopwords, but it must not own another regex loop for visible label words.
- Required Guardrail: Keep visible label-word extraction in `greenfield_domain_term_index.label_terms`. `greenfield_confirmed_components.py` may pass component-label stopwords and keep title casing with `_title_word`, but it must not reintroduce a local `for raw in re.findall` token loop for `domain_label`.
- Verification Added: `tests/unit/runtime/test_greenfield_component_spec_quality.py::test_confirmed_components_helper_shape_stays_below_soft_limit` now pins the shared label-term owner, absence of the local regex loop, and visible behavior for `3D GIS`, `AI CRM`, and prompt-derived repair-request labels. Replayable proof: syntax proof passed for the touched modules and test; focused component-spec proof passed (`12 passed in 0.25s`); wider greenfield proof passed (`151 passed in 233.14s`); confirmed-create performance proof passed (`1 passed in 11.90s`).
- Agent Guardrails: For future generated title, project-label, or component-label fixes, preserve display-label semantics through `greenfield_domain_term_index.label_terms` instead of forcing semantic normalization or adding private token loops to renderers.

## 2026-06-04 Follow-up: Experience Handoffs Still Forked Workstream Match Terms

- Fresh Finding: `greenfield_experience.py` still carried a private `_meaningful_terms` regex helper for matching generated component handoffs to child workstream titles after generated-artifact term normalization had moved to `greenfield_domain_term_index.ordered_terms`.
- Generic Trigger Path: Maintain `greenfield create --confirm` when generated Registry component handoffs need the right Radar child workstream and first-slice text across arbitrary product vocabulary.
- Additional Root Cause: Component handoff matching looked like local scoring glue, so it kept its own minimum-length token set instead of using the shared term index. That left plural labels such as `dashboards`, `targets`, or `windows` outside the same normalization path used by Radar, Registry, Atlas, Tribunal, and quality gates.
- Additional Invariant Violated: Generated handoff matching may own match thresholds and stopwords, but reusable title/label token extraction must share the greenfield domain term index. Local regex token loops in the handoff owner can make Radar-to-Registry assignment drift from the rest of generated artifact quality.
- Required Guardrail: Keep component handoff title matching on `greenfield_domain_term_index.ordered_terms` with caller-owned handoff stopwords and `minimum=4`. `greenfield_experience.py` must not reintroduce `_meaningful_terms` or local `re.findall` token loops for title/component matching.
- Verification Added: `tests/unit/runtime/test_greenfield_experience_terms.py::test_experience_handoff_terms_use_shared_domain_index` pins the shared owner, absence of the local regex loop, plural folding for `status dashboards`, and stopword folding for `reviews service`. Replayable proof: syntax proof passed for the touched module and test; focused experience/row proof passed (`2 passed in 0.08s`); wider greenfield proof passed (`152 passed in 229.35s`); confirmed-create performance proof passed (`1 passed in 11.57s`).
- Agent Guardrails: For future generated component handoff, first-slice, or workstream assignment fixes, pass caller-owned stopwords into `greenfield_domain_term_index.ordered_terms` instead of adding local token loops to the handoff owner.

## 2026-06-04 Follow-up: Traceability Still Forked Semantic Token Matching

- Fresh Finding: `greenfield_traceability.py` still carried a private `_semantic_tokens` regex helper for generated Radar, Registry, and Atlas traceability matching after generated-artifact term normalization had moved to `greenfield_domain_term_index.ordered_terms`.
- Generic Trigger Path: Maintain `greenfield create --confirm` when generated components, workstreams, and diagrams need mutual traceability across arbitrary product vocabulary and plural or compound labels.
- Additional Root Cause: Traceability scoring legitimately owns overlap thresholds and compound identifier expansion, but reusable term extraction stayed embedded in that scoring helper. That left plural component labels such as `Status Windows` outside the same normalization path used by Radar, Registry, Atlas, Tribunal, quality gates, and handoff text.
- Additional Invariant Violated: Generated traceability matching may own scoring, fallback parent assignment, and compound token expansion, but reusable semantic token extraction must share the greenfield domain term index. Local regex token loops in traceability can make Radar-to-Registry-to-Atlas links drift from the rest of generated artifact quality.
- Required Guardrail: Keep traceability semantic token extraction on `greenfield_domain_term_index.ordered_terms` with caller-owned traceability stopwords and `minimum=3`. `greenfield_traceability.py` may expand compound identifiers for matching, but it must not reintroduce local `re.findall` token loops for generated traceability semantics.
- Verification Added: `tests/unit/runtime/test_greenfield_traceability_terms.py::test_greenfield_traceability_terms_use_shared_domain_index` pins the shared owner, absence of the local regex loop, plural folding for `status dashboards`, compound expansion for `source-backed`, and component-to-workstream linkage from `Status Windows` to `Build window proof`. Replayable proof: syntax proof passed for the touched module and test; focused traceability/artifact proof passed (`41 passed in 162.78s`); wider greenfield proof passed (`153 passed in 230.09s`); confirmed-create performance proof passed (`1 passed in 11.77s`).
- Agent Guardrails: For future generated traceability, component-workstream assignment, or diagram linkage fixes, pass caller-owned stopwords into `greenfield_domain_term_index.ordered_terms` and keep only scoring policy plus compound expansion in the traceability owner.

## 2026-06-04 Follow-up: Confirmed Radar Backlog Text Still Forked Product Terms

- Fresh Finding: `greenfield_confirmed_backlog_text_model.py` still carried local regex term loops in `semantic_words` and `shares_product_terms` after generated-artifact vocabulary normalization had moved to `greenfield_domain_term_index.ordered_terms`.
- Generic Trigger Path: Maintain `greenfield create --confirm` when accepted Product Intent text generates Radar parent and child workstream first slices, problem text, opportunity text, and success metrics across arbitrary domains.
- Additional Root Cause: The Radar backlog text model owns grammar and first-slice wording, so its product-term comparisons looked like local text glue. That left plural terms such as `windows`, `services`, and `trails` outside the normalization path already used by Registry, Atlas, Tribunal, quality gates, traceability, and handoff text.
- Additional Invariant Violated: Confirmed Radar backlog wording may own stopwords and first-slice decisions, but reusable semantic term extraction and product-term overlap must share the greenfield domain term index. Local regex token loops can make Radar workstream text repeat visible outcomes or miss a legitimate match only because one side is plural.
- Required Guardrail: Keep confirmed Radar backlog term extraction on `greenfield_domain_term_index.ordered_terms` with caller-owned backlog stopwords and product-share stopwords. `greenfield_confirmed_backlog_text_model.py` must not reintroduce local lower-case `re.findall` token loops for `semantic_words` or `shares_product_terms`.
- Verification Added: `tests/unit/runtime/test_greenfield_confirmed_backlog_terms.py::test_confirmed_backlog_terms_use_shared_domain_index` pins the shared owner, absence of the old lower-case regex loops, plural folding for `status windows` and `review services`, compound term preservation for `source-backed`, and product-term sharing between `status windows` and `window proof`. Replayable proof: syntax proof passed for the touched module and test; focused backlog text proof passed (`11 passed in 13.37s`); wider greenfield proof passed (`154 passed in 228.69s`); confirmed-create performance proof passed (`1 passed in 12.05s`).
- Agent Guardrails: For future confirmed Radar backlog wording, first-slice generation, or success-metric matching fixes, pass caller-owned stopwords into `greenfield_domain_term_index.ordered_terms` instead of adding local token loops to the backlog text model.

## 2026-06-04 Follow-up: Registry Fallback Term Windows Still Lived In Contract Differentiation

- Fresh Finding: `greenfield_component_contract_differentiation.py` still owned `_literal_label_compounds` and `_nearby_content_terms` after Registry component phrase, axis, quality, and term-index helpers had moved into shared component term owners.
- Generic Trigger Path: Maintain `greenfield create --confirm` when generated Registry component contracts fall back from semantic axes and must still produce component-local owned state, accepted inputs, outputs, and failure proof across arbitrary component labels and product vocabulary.
- Additional Root Cause: Fallback-axis repair legitimately belongs in contract differentiation, so nearby context matching looked like local repair glue. That left reusable component label compound extraction and context-window token parsing inside a module that should only decide when and how to repair interchangeable contracts.
- Additional Invariant Violated: Registry contract differentiation may own fallback-axis scoring, sibling context, and repair decisions, but component label compounds and nearby context windows are reusable term-window behavior. Local regex token loops in the differentiation owner can drift from shared plural folding and can push near-limit component term modules past source-size discipline.
- Required Guardrail: Keep Registry fallback component label compounds and nearby context windows in `greenfield_component_term_windows.py`. `greenfield_component_contract_differentiation.py` must not reintroduce `_literal_label_compounds`, `_nearby_content_terms`, or local token regex loops for fallback component label/context matching.
- Verification Added: `tests/unit/runtime/test_greenfield_component_spec_quality.py::test_component_contract_phrase_helpers_stay_in_terms_owner` now pins the dedicated term-window owner, absence of the local helper definitions and regex loops in contract differentiation, short label compound behavior for AI CRM, plural folding for `status windows`, and all touched line-count limits. Replayable proof: syntax proof passed for the touched modules and test; focused component-spec proof passed (`12 passed in 0.25s`); wider greenfield proof passed (`154 passed in 224.99s`); confirmed-create performance proof passed (`1 passed in 11.72s`).
- Agent Guardrails: For future generated Registry fallback contract, component-local term, or interchangeable-spec fixes, move reusable label/window parsing into `greenfield_component_term_windows.py` or another focused term owner instead of adding private token helpers to contract differentiation.

## 2026-06-04 Follow-up: Registry Literal Label Terms Still Forked Across Contract Builders

- Fresh Finding: `greenfield_component_contract.py` still owned `_literal_label_terms` through `_label_compound_focus`, and `greenfield_component_contract_fields.py` still exported another `literal_label_terms` regex helper after `greenfield_component_term_windows.py` became the shared label/window owner.
- Generic Trigger Path: Maintain `greenfield create --confirm` when generated Registry component contracts derive owned-state phrases from component labels such as status windows, policy guardrails, reviewer notes, dashboards, queues, journals, and evidence records across arbitrary domains.
- Additional Root Cause: Base contracts, semantic contracts, and fallback differentiation each had legitimate contract wording responsibilities, so label-term extraction stayed close to the contract field that consumed it. That left the same visible component label parsing split across multiple Registry contract builders.
- Additional Invariant Violated: Registry contract builders may own wording, ranking, and field policy, but reusable component label term extraction must stay in `greenfield_component_term_windows.py`. The label-term owner must preserve short domain labels and plural artifact-carrier label nouns when the visible component label uses them, while still folding ordinary semantic plurals such as `windows`.
- Required Guardrail: Keep `literal_label_terms` and `literal_label_compounds` in `greenfield_component_term_windows.py`. `greenfield_component_contract.py`, `greenfield_component_contract_fields.py`, and `greenfield_component_semantic_contract.py` must not reintroduce local literal-label regex helpers, pass-through wrappers, or caller-local label compound extraction.
- Verification Added: `tests/unit/runtime/test_greenfield_component_spec_quality.py::test_component_contract_phrase_helpers_stay_in_terms_owner` now pins the shared literal label-term owner, direct imports from base and semantic contract builders, absence of local helpers in contract and field modules, plural artifact-carrier preservation for `policy guardrails`, ordinary plural folding for `status windows`, and all touched line-count limits. Replayable proof: syntax proof passed for the touched modules and test; focused component/guardrail proof passed (`13 passed in 3.38s`); wider greenfield proof passed (`154 passed in 216.42s`); confirmed-create performance proof passed (`1 passed in 10.18s`).
- Agent Guardrails: For future generated Registry component title, owned-state, or semantic-contract fixes, extend `greenfield_component_term_windows.py` with caller-owned options instead of adding private label token loops to contract builders.

## 2026-06-04 Follow-up: Registry Artifact Cleanup Still Forked Actor Role Detection

- Fresh Finding: `greenfield_component_terms.py` used a private suffix-only `_looks_actorish_term` check while `greenfield_component_semantic_context.py` carried its own `_looks_actor_term` role list. The base artifact cleaner also checked only canonical or past-tense actions, so inflected actor/action phrases such as `inspector reviews permit note`, `student submits assignment details`, and `operator approves safety guardrails` could survive as generated Registry artifact nouns.
- Generic Trigger Path: Maintain `greenfield create --confirm` when accepted first paths and component descriptions mention human actors performing actions before naming the product artifact the Registry contract should own.
- Additional Root Cause: Actor/action stripping started as local cleanup glue in two Registry contract paths. Once generated Registry contracts became a fail-closed anti-slop surface, local role classifiers and non-cached action-form checks became a shared artifact-quality policy, not caller-specific text cleanup.
- Additional Invariant Violated: Reusable actor-role token detection and action-form classification for generated Registry artifact phrases must have one owner. Component artifact cleanup and semantic context extraction may own their phrase ranking policy, but they must not carry separate role lists or action-form token loops.
- Required Guardrail: Keep actor-role token detection in `greenfield_actor_terms.py` and keep cached action-form classification in `greenfield_component_terms.py`. `greenfield_component_semantic_context.py` must import the shared actor classifier, and `greenfield_component_terms.py` must use action forms when dropping actor/action leads from artifact phrases.
- Verification Added: `tests/unit/runtime/test_greenfield_component_spec_quality.py::test_component_contract_phrase_helpers_stay_in_terms_owner` now pins the actor-term owner, cached action-form API, absence of `_looks_actorish_term`, and behavior for `inspector reviews permit note`, `student submits assignment details`, and `operator approves safety guardrails`. `tests/unit/runtime/test_greenfield_component_semantic_contract_quality.py::test_component_semantic_context_stays_in_dedicated_owner` pins semantic-context adoption of the shared actor classifier. Replayable proof: syntax proof passed for the touched modules and tests; focused component proof passed (`14 passed in 0.14s`); wider greenfield artifact proof passed (`156 passed in 195.31s`); confirmed-create performance proof passed (`1 passed in 9.29s`).
- Agent Guardrails: For future generated Registry artifact, semantic-context, or component-contract text cleanup, extend `greenfield_actor_terms.py` or `greenfield_component_terms.py` instead of adding local role classifiers, private actor suffix checks, or non-cached action-form token loops to contract builders.

## 2026-06-04 Follow-up: Registry Contract Gates Still Forked Generic Actor Labels

- Fresh Finding: `greenfield_component_contract_fields.py`, `greenfield_component_contract_differentiation.py`, and `greenfield_component_contract_quality.py` still carried local generic actor-label prefix regexes for operator, reviewer, owner, user, build, release, and end-user advocate wording after actor-role token detection moved into `greenfield_actor_terms.py`.
- Generic Trigger Path: Maintain `greenfield create --confirm` when generated Registry component contracts receive visible object labels such as `Primary user request status`, `Risk reviewer guardrails`, `Operator approval packet`, or public-gate labels such as `Build owner proof` across arbitrary product narratives.
- Additional Root Cause: Generic actor labels started as defensive wording inside individual contract gates. Once component fields, differentiation repair, and rendered quality all needed the same decision, the prefix list became a shared actor-term policy rather than local Regex glue.
- Additional Invariant Violated: Generic actor-label prefix detection and localization must have one owner. Contract fields may choose artifact focus, differentiation may choose repair targets, and quality may sentence-normalize fields, but they must not carry diverging actor-prefix regexes.
- Required Guardrail: Keep `generic_actor_label_prefix`, `starts_with_generic_actor_label`, and `localize_generic_actor_label` in `greenfield_actor_terms.py`. Component contract fields, contract differentiation, and component contract quality may call those helpers, but they must not reintroduce local generic actor-prefix lists or Regex literals for operator/reviewer/owner/user labels.
- Verification Added: `tests/unit/runtime/test_greenfield_component_spec_quality.py::test_component_contract_phrase_helpers_stay_in_terms_owner` now pins the shared generic actor-label owner, the component caller imports, absence of the old local actor-prefix Regex, behavior for artifact-bearing generic prefixes, and localization of bare generic actor labels. Replayable proof: syntax proof passed for the touched modules and test; focused component proof passed (`14 passed in 0.17s`); wider greenfield artifact proof passed (`156 passed in 194.91s`); confirmed-create performance proof passed (`1 passed in 9.55s`).
- Agent Guardrails: For future generated Registry contract field, differentiation, or rendered-quality work, extend the shared actor-term owner for generic actor labels instead of adding local actor-prefix regexes to contract builders.

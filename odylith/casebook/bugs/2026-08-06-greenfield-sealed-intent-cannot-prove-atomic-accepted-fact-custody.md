- Bug ID: CB-323

- Status: InProgress

- Created: 2026-08-06

- Severity: P0

- Reproducibility: Consistent

- Type: Product

- Description: The independently authored exact-distribution final holdout measured accepted_fact_custody at 0 of 136. The product seals custody for only five coarse fields while release truth annotates atomic actors, actions, states, outputs, constraints, dependencies, and non-goals. Directly supported atoms inside a larger field therefore cannot be proved, and persisted snapshots also dropped explicit prohibitions and named systems.

- Impact: Users can receive a polished pre-confirm package that cannot demonstrate which atomic product claims are directly supported, while critical source constraints and systems can disappear from canonical meaning.

- Components Affected: domain-intelligence

- Environment(s): Odylith 0.1.15 exact dist from aa51e5cf92da4daa79fd3aa40944d49a0211eb75; disclosed 24-case independent final holdout

- Detected By: Hash-bound one-shot semantic release proof plus independent adversarial review

- Failure Signature: The first disclosed holdout reported accepted_fact_custody=0/136, critical_constraint_recall=2/21, explicit_system_recall=2/7, and 24 P0 findings. A second disclosed 24-case holdout at 87e277d87 reported accepted_fact_custody=25/36, critical_constraint_recall=1/5, explicit_system_recall=1/4, and 7 P0 findings before scorer correction. Its market-stall case also compiled `Retain waitlist order` as the terminal first-path event, then failed because Atlas correctly omitted that false user step.

- Trigger Path: Run the sealed final-holdout release campaign against exact dist and score atomic annotations against preconfirm semantic snapshots

- Ownership: Greenfield canonical meaning, evidence custody, and semantic release scoring

- Timeline: Observed 2026-08-05 in one-shot revision-bound holdout; ledger terminal status failed and corpus retired to regression

- Blast Radius: Any Greenfield evidence with multiple accepted atomic facts, explicit systems, constraints, prohibitions, or non-goals

- SLO/SLA Impact: Blocks v0.1.15 release and invalidates semantic release-readiness claims

- Data Risk: No governed source data loss; semantic omission can misdirect every generated governance and implementation artifact

- Security/Compliance: Safety, authority, and compliance boundaries can be omitted or downgraded from accepted source facts

- Invariant Violated: Every accepted material fact must carry a valid source span, semantic entailment, polarity, custody state, and projection trace before confirmation

- Root Cause: Field-level material custody covered only product_story, state_object, first_path, proof_boundary, and human_actors; atomic categories had no sealed provenance ledger. After the ledger landed, natural-language external-boundary recovery still recognized only from/via/supplier forms, mixed-polarity source sentences assigned prohibited polarity to positive sibling clauses, and source-ranked workflow recovery treated adjacent positive policy obligations as user events. A command-led restatement could also leave `same` attached to the title candidate, recover its title from the path's terminal dependency, append a standalone `relies on` clause to the user path, and drop the positive side of a semicolon-separated mixed-polarity obligation. For actor-led prompts without an inline title, materialization always preferred a title inferred from the visible result even when its caller supplied a short non-action product title. The external-boundary supplier frame also treated object nouns such as `reports` as supplier verbs without checking whether an earlier action already owned the noun, allowing prompt framing such as `Create ... receives reports` to become a clipped external-system fact. The host-planned rescue path could then replace sealed semantic facts directly even though host output is advisory evidence, recreating the authority drift that the retired blanket re-seal path had hidden. Multi-step source paths could also be sealed before sentence-case canonicalization, allowing proposal completion to invalidate the authority hash. The release scorer trusted transaction-provided custody structure without independently rebinding projection paths, source hashes, and entailment to the scored evidence.

- Solution: Seal one domain-neutral atomic fact ledger with category, polarity, normalized value, source-span IDs, custody, and projection links; recover external boundaries through bounded typed relation frames; require supplier-frame subjects to be action-free so object nouns do not become systems; split source custody into local polarity units; project standalone positive policy obligations into operational constraints instead of the user path; treat referential `same` as command framing only when a structural restatement follows; reject standalone non-human dependency relations from user-path ranking while preserving dependency clauses with user-action tails; preserve both orders of semicolon-separated mixed-polarity evidence; prefer a short non-action supplied fallback title over an inferred result title; canonicalize source-ranked paths before sealing; bind host-planned semantic repairs to the sealed Product Intent fact instead of accepting host output as new evidence; score the same units; preserve bounded_interpretation only for actual inference

- Rollback/Forward Fix: Forward fix only; no post-confirm semantic repair and no weakening of custody floors

- Verification: The source-local atom ledger, current and legacy envelope migration, authority sealing, scorer-forgery rejection, supporting-evidence exclusion, and transaction-body tamper tests pass. The disclosed 21-case retired commit corpus now preserves every annotated actor, action, state, output, dependency, and non-goal through prompt materialization and accepts every annotation in the sealed atom ledger; its dedicated cross-domain custody matrix passes 44 tests. The complete recovery, materiality, and requirement-boundary family passes 120 tests with no exclusions after repairing the two defects independently reproduced at the prior checkpoint: actor evidence in an edited First Complete Path no longer requires a redundant Human Actors section, and nominal list terms such as baseline routes and operator notes no longer become fabricated actions or actors. Atomic-ledger and Product Intent envelope proof passes 40 tests. Re-scoring the sealed 87e277d87 holdout outputs after clause-local polarity and category-bound recall correction raises critical_constraint_recall from 1/5 to 5/5 and reduces P0 findings from 7 to 3 without rerunning product behavior. The product now recovers the exact external-system set for all 11 disclosed commit cases and seals the four previously scoreable systems as accepted dependency facts, including a dependency adjacent to a prohibition. Two adversarial review rounds produced and closed counterexamples for comparison artifacts, conditional tails, modal supplier clauses, capitalization, people, device carriers, and product-title self-classification. The source-obligation correction now proves that bare, actor-led, compound, and evidence-bearing obligations stay source-entailable while complete `keep`/`preserve`/`retain` paths remain usable; the disclosed market-stall package passes its Atlas-tail tribunal and the logistics path preserves its authority hash. The disclosed canopy restatement now compiles as `Tree-canopy Ledger`, keeps all three declared domain terms, excludes `mapping gateway` from the user path while sealing it as an external system, retains the seven-year photo obligation, and preserves the non-scoring prohibition. The resulting proposal passes Tribunal. A final adversarial pass found and closed prohibition-first mixed evidence, dependency-plus-action tails, and legitimate `Same Day Ledger` title counterexamples. Post-review proof passes 90 high-variance and external-boundary tests, 194 fast Greenfield tests with eight intentional deselections, 11 exact counterexample tests, and all 319 Casebook records. The final supplied-title correction passes its five actor-led variants plus two outcome-title controls; the complete disclosed retired-holdout and workflow-custody gate passes all 148 tests with no exclusions. Exact installed dist `ad19c753c` then passed 13 maintained standard scenarios at 10/10 plus commit-recovery proof, but failed `sparse disclosure confirmation` because `reports` was parsed as a supplier verb and sealed a clipped external-system row. The same unsupported row caused the bounded structured-rescue patch to change sealed facts and fail strict authority binding; the provider-failure source-anchored fallback leg itself was observed. The correction passes 44 external-boundary tests, 76 focused boundary/planner/executor tests, and 195 fast Greenfield tests with eight intentional deselections. Full source-local sparse and host-planned rescue proposals now both compile verified transactions; the rescue manifest retains `structured_rescue_semantic_patch`, a planned Codex PatchSet, and the source-supported external-system fact without changing the sealed hash. One widened commit-manifest fixture still fails identically at clean `ad19c753c` because its Markdown actor label is canonicalized after its test authority is sealed; it is a pre-existing compatibility-fixture defect, not evidence against this slice. Exact installed dist `cf41046a9` run `edc92934f9c449cc882c1606cda61816` passes all 14 maintained scenarios at 10/10, including sparse disclosure and quantum communication; all 13 applicable generated browser workspaces pass, with the clarification-only case correctly marked not applicable. Real host-planned rescue, provider-failure fallback, SIGKILL recovery, same-hash retry, fsync failure rollback and retry, operator-conflict preservation, readback, and temporary-root cleanup all pass. CB-323 remains P0 and in progress only until a fresh blind holdout independently meets the same release floors.

- Prevention: Exercise real production semantic snapshots in scorer tests and reject 0-of-0 or synthetic accepted-fact fixtures as release proof

- Agent Guardrails: Do not add domain phrase lists, infer custody from generated-field adjacency, or make the scorer award accepted_fact without a source-entailment binding

- Preflight Checks: Retired regression corpus, cross-split leakage, all three profiles, all evidence styles, no-write clarification, browser, recovery, and exact distribution provenance

- Regression Tests Added: tests/fixtures/greenfield-release-corpus/retired-aa51-final-holdout-regressions.v1.json preserves the disclosed cases and atomic annotations; tests/unit/runtime/test_greenfield_prompt_workflow_custody.py binds the 21 commit-capable cases to canonical and atom-level custody expectations

- Monitoring Updates: Report extraction availability separately from package/render failure, and expose atomic custody denominators by category and profile

- Version/Build: 0.1.15 aa51e5cf92da4daa79fd3aa40944d49a0211eb75

- Related Incidents/Bugs: CB-302, CB-303, CB-315, B-142

- GitHub Status: confirmed

- Public Response: pending

- Code References: - src/odylith/runtime/domain_intelligence/greenfield_sealed_product_intent_authority.py
- src/odylith/runtime/domain_intelligence/greenfield_product_intent_envelope.py
- src/odylith/runtime/domain_intelligence/greenfield_atomic_fact_ledger.py
- src/odylith/runtime/domain_intelligence/greenfield_confirmed_intent.py
- scripts/release/greenfield_matrix_transaction_evidence.py
- scripts/release/greenfield_semantic_release_score.py

- Fresh Blind Holdout Reopen (2026-08-08): The independently authored and contract-validated 24-case holdout, SHA-256 `2713e5b4cbd0abe0c7cc1e517c063c29ca3cdd029c2db5de764ecc8c03c9cfb5`, ran once against exact dist `cf41046a9` under ledger `odylith-final-holdout-ledger-cf41046a9-v3`. Semantic release failed with accepted-fact custody `59/88` (`0.670455`, floor `1.0`), explicit-system recall `2/7` (`0.285714`, floor `1.0`), first-path comprehension `46/65` (`0.707692`, floor `0.9`), and overall case success `0/24`. Critical source constraints passed `7/7`, which isolates the remaining P0 custody defect instead of invalidating the entire ledger. Five P0 findings were missing explicit systems across four cases, including one case with two omitted systems. All seven transactions that reached confirmation committed through the commit-only path, and shared recovery proof passed, so this result does not reopen post-confirm generation or publication ownership. It proves that the pre-confirm canonical ledger and Tribunal still allowed incomplete meaning to be sealed. The disclosed holdout is retired; no rerun may be used as a fresh release claim.

- Fresh Blind Holdout Custody Follow-Up (2026-08-08): Source replay isolated the five missing systems to two domain-neutral evidence frames the external-boundary parser did not own: `use <named system>` and `<names> is/are external source/system`. The repair reads those frames with token grammar, rejects explicit negation and generic product/service subjects, and keeps dependency declarations outside human actors and the first path. Pasted evidence labels and compact `Confirmed direction` identity text now remain evidence framing rather than product title or workflow steps. The atomic ledger also preserves each coordinated subject in a shared required predicate, while prohibited shared predicates cannot promote either noun to accepted truth. All five named systems from the four failed cases now appear as exact `accepted_fact` dependencies with source refs. The combined atomic, envelope, external-boundary, prompt-source, workflow-custody, and disclosed-corpus pack passed 314 tests in 412.23 seconds. This is regression evidence only; a new exact distribution and untouched holdout remain required.

- Adversarial Custody Closure (2026-08-09): Review found that comma, `or`, and Oxford-comma declarations could still lose subjects, capitalized human roles could be misclassified as systems, and a compact `Confirmed direction` heading produced no operator-evidence spans. Shared declaration parsing now preserves every affirmed subject and rejects prohibited predicates; human-role grammar remains outside the system boundary. Compact confirmation content stays under the operator-evidence section, so materiality passes from source-backed bounded interpretation and exact actor, action, state, output, constraint, non-goal, and dependency atoms retain accepted source refs. The first source-only classifier revision also erased SkyTrace's `screening state`; the final rule excludes only terminal supplier clauses and preserves system-owned state transitions. The complete custody, envelope, workflow, and disclosed-holdout pack passed 329 tests in 492.20 seconds, including the corrected orbit counterexample. This remains regression evidence only; release still requires a new exact distribution and untouched holdout.

- Replacement Blind Holdout Reopen (2026-08-16 UTC): The independently authored replacement holdout ran once against exact candidate `1ba7b4c0385ed36981c5d163439a49f3be703661` and failed with accepted-fact custody `30/97` (`0.309278`, floor `1.0`), critical-constraint recall `25/28` (`0.892857`, floor `1.0`), explicit-system recall `9/16` (`0.5625`, floor `1.0`), first-path comprehension `25/53` (`0.471698`, floor `0.9`), and semantic overall success `0/24`. The scorer emitted ten P0 atomic findings: seven explicit-system omissions and three critical-constraint omissions. Package proof separately found ten domain-term failures; independent adjudication classified seven as P0 semantic replacement or leakage and three as P1 preservation misses, with no evaluator or oracle reversals. Recovery, commit-only publication, platform leakage, and temp cleanup passed, isolating the release blocker to pre-confirm canonical custody and projection. The terminal result SHA-256 is `180437fd4000e8c46eefe19b1e44c0fce14fb53e6666bf18838a1894d7a2a988`; adjudication SHA-256 is `fa81048b515bdc0db98fd04c6ce8205ee7964e5ee5be9d862d0ce191f308838d`. The disclosed corpus must become regression evidence before another blind holdout.

- Exact-Commit Final-Edit Custody Review (2026-08-16): Independent review of source checkpoint `0663a48f472e56789f04451fc49a482d2cb750d7` found one P1 counterexample after the replacement-holdout repairs: a title-only `Final edit:` preserved a previously named external boundary through boundary-specific fallback but unconditionally reduced rankable prompt evidence to the revision suffix. Prompt source therefore lost the accepted actor and first path, and materialization substituted representative-user fallback truth. The correction makes revision authority field-local: a suffix with explicit workflow fields or an actor-owned workflow replaces prior workflow evidence, while title-only and other non-workflow edits compose with the accepted pre-edit evidence. Exact end-to-end proof preserves the source actor, action, visible result, and named dependency without generic fallback; an explicit Actor plus First path control replaces the earlier workflow while retaining the dependency. Focused proof passed `2/2`; the workflow-custody, high-variance, structured-first-path, and external-boundary compatibility pack passed `231/231` in `402.44s`; Python compilation and diff hygiene passed. Failed mechanism to avoid: do not treat a document-level revision marker as authority to erase semantic categories the suffix does not revise.

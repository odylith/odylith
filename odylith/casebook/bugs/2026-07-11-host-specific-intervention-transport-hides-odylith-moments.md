- Bug ID: CB-242

- Native Automatic Confirmation (2026-09-08): Fresh native session 01a084c6-a193-7c43-9cec-e45db199ab35 displays a complete Observation and Assist in its final answer to the same scoped read-only prompt. Stop itself confirms both through codex_stop/assistant_chat_transcript: two confirmed, zero pending, proven_this_session. No manual visibility or confirmation command, configuration override, edit, test, delegation or holdout access occurs inside that probe; no Stop budget warning is observed. The turn takes 41.431 seconds, not an isolated Stop measurement. The earlier instrumented comparison remains the timing evidence. All seven reviewed source/test files match in the proof checkout, where 60 focused controls pass. Original runtime has 3,557 passes and the unchanged renderer-size baseline failure. The full desktop/mobile governance browser matrix has 202 passes and two skips for absent Radar traceability diagnostics and Compass detail links. Source/test bytes remained frozen; normal hook-driven governance upkeep was active during the matrix. Activation remains unverified, and current-desktop activation, newly generated Stop closeout, purpose-built customer copy, installed release and full Greenfield quality remain open. Evidence: /private/tmp/odylith-active-intervention-repair.HfhM0b/native-copy-probe.json, native-copy-status.json, runtime-recorded-copy.xml, browser-recorded-copy.xml and proof-recorded-copy.xml.

- Stop Cost Attribution (2026-09-08): The retained native final message, replayed through the actual source-local Stop owner with its unchanged 14-second deadline, spends 0.000 seconds in deferred settlement, 3.318 seconds logging, then 10.676 seconds preparing the conversation bundle before expiry. The call chain unexpectedly enters live architecture benchmark evaluation and projection compilation through the nominally compact runtime summary. The bounded replacement reads the existing Compass runtime summary, explicitly marks it recorded with unverified freshness, respects the current disabled switch, and never rebuilds on absent or corrupt evidence. The same instrumented check now completes in 3.837 seconds, including a 0.468-second bundle; 46 focused controls pass. This comparison is local execution, not fresh native Stop chat proof. Keep the native activation, copy-quality and broader validation gates open. Do not increase deadlines, disable engines or treat a refreshed Compass wrapper timestamp as the age of cached evidence. Evidence: /private/tmp/odylith-active-intervention-repair.HfhM0b/profile_stop.py.

- Copy Ownership Diagnosis (2026-09-08): Independent tracing reproduces the malformed native Observation in the shared voice formatter. The complete Casebook Description becomes a fact consequence, then a 220-character window cuts it at a comma and the sentence formatter adds a period. The cut removes a meaningful continuation-loss clause and cross-host consequence. Remove the lossy shared operation and preserve complete selected propositions; retain fact-count limits and duplicate suppression. Do not strip commas, expand the limit or add text-repair rules. This correction does not by itself rewrite diagnostic source wording into purpose-built customer copy.

- Fresh Native Delivery Probe (2026-09-08): After all five trust approvals, one read-only Codex CLI prompt in session 01a084ac-61a8-7061-a2e2-46a6bcc56380 automatically produced an Observation and Assist in assistant commentary. The only model tool actions searched/read this Casebook record; no manual visibility command, edit, repair, test, delegation or holdout access occurred. The actual native transcript was then supplied to the existing confirmation API, which confirms two visible events with no pending events. This is fresh prompt-delivery evidence in that CLI session, not current-desktop activation or full closeout proof. Stop visibly deferred at its 14-second foreground boundary, and the Observation ends with malformed comma-period punctuation. No dirty-event ledger is present after the probe; do not assume deferred checkpoint backlog caused the timeout. Codex status still reports activation unverified. Keep this defect open for concise, coherent generated copy, Stop phase-cost attribution and native activation attestation; do not fix the copy by accumulating string-repair rules or expand the time budget. Evidence: /private/tmp/odylith-active-intervention-repair.HfhM0b/native-probe.json and native-probe-confirmation.json.

- Delegated Review Completed (2026-09-08): All five exact project hooks are now trusted/enabled through the supported Codex CLI review UI, including Stop after the bounded cleanup correction. A separate native hooks/list confirms five trusted/enabled definitions, their hashes and timeouts, with zero errors and no configuration override. No model prompt was needed to grant trust. Frozen validation: 3,540 runtime passes with one unchanged baseline hotfile failure; 1,093 install passes with three Python-invocation failures and two obsolete flat-hook assertions. The affected subprocess modules pass all 28 controls on the repository toolchain, and both wrapper assertions pass after matching the already validated native wrapper contract. Intervention/dashboard browser controls pass 42 checks in each checkout; the final proof repair passes 33 focused controls. The original Atlas catalog has pre-existing dangling references; the proof checkout's full governed refresh passes. These unresolved baseline gates are not waived. Native execution and actual chat delivery remain unverified; trust alone does not close this defect. Evidence: /private/tmp/odylith-active-intervention-repair.HfhM0b/.

- Browser Cancellation Evidence (2026-09-08): The unchanged real Mermaid probe first reproduced four surviving browser processes after hook expiry. Those exact probe-owned processes were cleaned up. The smallest correction preserves the existing 0.2-second TERM grace for the owned group even after its leader exits, allowing the existing Node cleanup handler to finish; no Mermaid, Puppeteer option or supervisor was added. The identical positive/negative pair now completes normally in 1.446 seconds and cancels in 14.207 seconds, with no recorded processes live immediately or after 0.5 seconds. A real asynchronous-child cleanup regression fails before the change; 27 focused controls pass afterward. The cancelled browser profile remains on disk and was preserved recoverably in the evidence directory; profile cleanup and arbitrary shutdown latency are not claimed solved. Evidence: /tmp/odylith-mermaid-grace-proof.iR2d9A/README.md. Native trust/execution/chat remain separate gates.

- Native Review Progress (2026-09-08): Under the operator's explicit delegated review authority, the maintainer reviewed exact definitions and used the documented native CLI hook UI to trust PreToolUse, PostToolUse, SessionStart and UserPromptSubmit individually. A fresh native hooks/list confirms these four trusted/enabled, Stop still untrusted/enabled, and zero discovery errors. No bypass flag, private trust-store edit, model prompt or hook-disable setting was used. The review-only CLI exited cleanly. Registration/trust are not automatic chat proof. Stop is withheld pending the separately grouped browser cleanup boundary below; the operator does not need to perform the code audit.

- Bounded Stop Ownership Proof (2026-09-08): One shared foreground runner now serves both hosts; Codex Stop applies a 14-second whole-body deadline, preserves caller timers, and reports nonblocking deferral with unsettled events retained. A real sync renderer escape is reproduced before correction; timed sync children now inherit the already owned foreground group only in this process-local hook scope. Standalone sync, unrelated siblings and independent bounded background engines retain their existing ownership. 173 focused controls pass in 31.11 seconds. A controlled launcher-shaped Stop invocation completes in 14.543 seconds measured before Popen, retains pending events, emits no block decision, and stops its sync/renderer/grandchild group. This is fixture execution, not native hook/chat proof. Puppeteer browser children form another group; its public launcher does not forward detached:false, so that proposed option would be ineffective and was not added. Obtain real browser cancellation evidence before extending the repair; do not turn a theoretical risk into an unlimited hardening program. Evidence: /private/tmp/odylith-active-intervention-repair.HfhM0b/ and the three new runtime hook budget/ownership test modules.

- Delegated Trust Review (2026-09-08): The operator asks the maintainer agent to review the five definitions and proceed if safe; do not shift the code audit back to the operator. Review finds no arbitrary prompt-to-shell execution or malicious hook command. Prompt/command reads, local governance writes and configured background provider work are real authority, not notification-only behavior. One synthetic control proves raw inline command content is duplicated into the dirty-event ledger; remove the unused command field and its writer argument, retaining event identity, session, paths and settlement behavior. The failing privacy control now passes together with 39 checkpoint/Stop controls (40 passes, 4.60 seconds). Historical ledgers are not inspected or rewritten. Stop still permits a 20-second start plus a 180-second sync beneath a 20-second host hook; owned-child cancellation and end-to-end timing remain open, so blanket trust and automatic-delivery claims are withheld. Native protocol discovery exposes hooks/list but no hook trust mutation method; do not bypass trust or edit private trust storage. Evidence: /private/tmp/odylith-active-intervention-repair.HfhM0b/.

- Disabled Integration Follow-up (2026-09-08): Independent review finds install, same-version upgrade, staged upgrade and rollback completion refreshes omitted the stored integration flag. A disabled-integration reinstall control reproduces unintended host-registration mutation (one failure, four passes); all four completion sites now carry integration_enabled, matching doctor. Five real lifecycle controls pass in each checkout, including doctor, reinstall and source-local upgrade with integration remaining disabled. This prevents configuration mutation while off; it does not claim the prior reinstall changed the stored integration flag.

- Active Registration Repair (2026-09-08): The product-repo doctor omission is reproduced by a real install/repair control: one failure, two passes before correction; all three pass afterward. Product host refresh now reuses the existing preserving Codex/Claude settings writers while leaving authored guidance alone, and doctor passes the stored integration-enabled state so repair cannot reenable disabled hooks. Matching validated hook, compatibility, consumed-decision and closeout owners are backported into the actual source-local checkout. Its supported doctor repair completes, preserving root guidance, Codex config and Claude settings, and wrapping all existing hook entries unchanged. All 183 focused checks pass in 35.87 seconds. Fresh native hooks/list under the actual existing configuration discovers five enabled project hooks with zero discovery errors, all untrusted; no trust override or model turn occurs. Actual status advances from degraded to unverified with all six static checks true. The user must review exact definitions before fresh-session execution and chat visibility can be qualified. Evidence: /private/tmp/odylith-active-intervention-repair.HfhM0b/. Do not close this defect or describe discovery as automatic delivery.

- Active Checkout Repair Finding (2026-09-08): Explicit operator repair authorization reopens the matching-runtime rollout. The active checkout at e139ff3e still has flat Codex hooks and pre-fix source readers. A bounded independent review identifies an additional local omission: doctor delegates host refresh to refresh_consumer_managed_guidance, whose product-repo early return skips host settings entirely. Preserve authored product guidance while invoking the existing host-settings writers when activation is requested; do not grant native trust, overwrite custom settings, or enable an intentionally disabled integration. The initial backport test run has 85 failures and 95 passes; after the five shared owners, eight failures remain, including the compatibility report, the still-flat live hooks, and two branch-specific test-fixture differences. These are not independent native defects. Evidence: /private/tmp/odylith-active-intervention-repair.HfhM0b/. Keep native activation and visible-chat proof open.

- Status: Open

- Created: 2026-07-11

- Severity: P1

- Reproducibility: Consistent

- Type: UX

- Description: The visible-intervention fallback can show diagnostic host/tool language, prompt eligibility is too keyword-bound for ordinary UX feedback, and the Codex stop continuation request is discarded before transport. This makes useful Odylith observations and assists inconsistent across Codex, Claude, and future hosts.

- Impact: Operators do not reliably see useful Odylith guidance and may see internal delivery language when they do.

- Components Affected: intervention-engine

- Environment(s): Product-repo maintainer source-local and installed host hook paths

- Detected By: Operator screenshot and adversarial code review

- Failure Signature: Codex visible fallback emits host-specific diagnostic prose; Claude has no equivalent automatic visible delivery; stop continuation flag is dropped.

- Trigger Path: Prompt submit, hook stop, and visible-intervention recovery workflows

- Ownership: Intervention Engine host-surface boundary

- Timeline: Captured 2026-07-11 through `odylith bug capture`.

- Blast Radius: All supported and future model hosts

- SLO/SLA Impact: Breaks the observable intervention UX contract and delays user decisions.

- Data Risk: User-facing trust and clarity risk; historical dirty-event records may retain unnecessary command text. The new writer omits that field and leaves historical logs unchanged.

- Security/Compliance: Hook authority requires explicit native trust. Installer/repair must preserve disabled integration and cannot approve its own hooks.

- Invariant Violated: A shared, useful, host-neutral intervention plan must survive transport and never expose internal hook state.

- Root Cause: Presentation, eligibility, and transport policy are distributed across host adapters instead of sharing one visible-moment contract.

- Solution: Introduce a host-neutral visible-moment plan, preserve continuation delivery intent, broaden earned user-feedback eligibility, and prove cross-host rendered parity.

- Rollback/Forward Fix: Forward fix with regression and browser coverage; preserve silent success hooks where required.

- Verification: Cross-host exact Markdown parity, stop continuation transport, ordinary UX-feedback eligibility, and browser normal/empty/degraded scenarios.

- Prevention: Keep user-facing copy in a shared planner and restrict host adapters to transport metadata.

- Agent Guardrails: Never render host names, hooks, tools, or delivery ledger state in customer-facing intervention copy.

- Preflight Checks: Inspect intervention status and visible fallback before claiming chat-visible UX.

- GitHub Status: confirmed

- Public Response: pending

- Code References: - src/odylith/runtime/intervention_engine/visibility_broker.py
- src/odylith/runtime/intervention_engine/host_surface_runtime.py
- src/odylith/runtime/intervention_engine/prompt_signal_runtime.py

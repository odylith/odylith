- Bug ID: CB-304

- Native Fault Boundary (2026-09-08): Claude 2.1.202 invokes a command hook with a one-second timeout, but an injected three-second delay produces no callback output and lets CONFIRM reach one loopback messages request. No publication occurs. Successful callbacks cannot qualify unconditional no-post-confirm inference when their transport fails open. Do not add retries or model narration around this boundary. A separate Codex exit-code-2/stderr control blocks inference but still hides the full receipt; reject it as a visibility improvement. Evidence: /private/tmp/odylith-claude-confirmation.5ntA3g/results-timeout-text.json and /private/tmp/odylith-native-confirmation-control.xvEV2h/results-production-exit-two-text.json. All native children and detectors are terminal.

- Settled Regression Evidence (2026-09-08): After the owned forensic refresh and two obsolete test-lookup corrections, runtime has 4521 passes and installation has 1382 passes with zero failures/errors. The browser run records 274 completed passing nodes before a terminal BrokenPipeError in pytest output reporting. Exact collection identifies 53 unexecuted nodes; their separate rerun passes 52 and skips one absent-fixture diagnostic case. All 327 collected nodes are accounted for (326 passes, one skip), without changing the product tree during recovery. Preserve both XML receipts and browser-resume-selection/result JSON under /private/tmp/odylith-native-confirmation-control.xvEV2h/. This settles regression coverage, not native host eligibility, fault safety, live quality or release readiness.

- Native Claude Publication And Visible Receipt (2026-09-08): Claude Code 2.1.202 invokes the real prompt-bundle callback in an isolated configuration with a loopback-only API detector. The context-only comparison makes one inference request; the production unavailable-hash controls make none and show the complete outcome in both native text and stream output. A separate nonempty proposal stages 95 sealed writes using fixed author and independent review transport doubles, then native CONFIRM publishes through the unmocked kernel. Independent readback matches all 96 after-image files and modes. Native retry returns identical callback output and preserves all 214 publication, pending and journal files and modes. Both publication turns show the complete receipt and dashboard path with zero inference requests. Browser auto-open and process/socket creation inside the callback are disabled. This qualifies bounded source-local Claude execution and text visibility, not live semantic quality, installed activation, command-hook timeout safety or Codex desktop visibility. All control children and the local detector are terminal. Evidence: /private/tmp/odylith-claude-confirmation.5ntA3g/results-publication-text.json and the three negative/control receipts beside it.

- Expanded Validation Settlement Required (2026-09-08): The runtime suite terminates with 4520 passes and one stale Release FORENSICS sidecar failure in 403.86 seconds. Installation unit/integration terminates with 1380 passes and two old flat-document assertions in 1033.73 seconds. Actual installed output has the native hooks wrapper; update those test lookups, not the repaired emitter. Source-local Registry check-only independently reproduces the stale sidecar. Apply the owned synchronizer after all validation handles are terminal, then rerun. The retained 3159-file fingerprint is unchanged across the observed frozen interval, but its first measurement followed test startup; these receipts are not an atomic before-run freeze. Prior forensic settlement learning is CB-270; this occurrence does not establish its historical pinned-runtime cause.

- Native Generated Hook Discovery (2026-09-08): A discovery-only real Codex app-server run reads the exact repaired renderer output and lists all five production commands as project hooks with their expected timeouts and matchers. Every hook remains untrusted; no turn is started and no inference request occurs. This proves native document discovery, not automatic trust, execution or visible completion. Preserve /private/tmp/odylith-native-confirmation-control.xvEV2h/app-server-generated-discovery/result.json. Do not edit user trust records or offer governed writes based on discovery alone.

- Native Registration Format Defect Confirmed (2026-09-08): Correctly trusted file controls distinguish the shipped flat event map from the documented wrapper. Flat hooks.json produces no hook witness and reaches one local response request; wrapped hooks.json invokes UserPromptSubmit and ends the turn without inference. Both use the same hook and real Codex 0.153.1 with one-off vetted hook trust. The shared emitter, writer, capability reader and intervention-status reader currently assume flat maps, so presence checks can misreport unusable registration as wired. Fix that shared document boundary and installed/live mirrors, preserving custom hooks, metadata, backups and idempotency. This does not grant trust, establish desktop completion visibility or qualify semantic quality. Receipt: /private/tmp/odylith-native-confirmation-control.xvEV2h/results-git-trusted-table.json.

- Native Nonempty Publication And Retry (2026-09-08): A real pre-confirm compiler, with fixed author and independent admitting-review transport doubles only, stages 95 writes over an explicit published baseline. The actual Codex hook invokes the unmocked kernel, publishes and returns CLOSED. Independent byte and mode comparison covers all 96 after-image files. A second native exact-hash invocation returns identical callback output; all 204 publication/journal files and the seal remain unchanged. Both turns make zero inference requests; process and socket creation are forbidden inside the callback and browser auto-open is disabled. This qualifies the bounded source-local native transaction path, not semantic quality, installed registration, human-visible completion, Claude parity or the timing distribution. Evidence: results-production-full.json under /private/tmp/odylith-native-confirmation-control.xvEV2h/.

- File Discovery Control Correction (2026-09-08): The app-server preflight demonstrated that quoting a segment inside a dotted CLI override creates a literal quoted key instead of the intended key. Earlier project trust controls used the same construction, including dots inside absolute paths. Compare the same two file shapes with the entire projects table supplied as one TOML value; do not change the product hook format unless this corrected control actually discovers and invokes it.

- Independent Review Fixture Wiring (2026-09-08): With the baseline present, the old HIIT integration helper still cannot stage because its provider substitution returns the author response for both authoring and the now-required independent candidate_review role. This is obsolete transport-double wiring, not evidence to change source interpretation, skip review or patch domain vocabulary. Bind the existing AdmittingReviewProvider only to the review role for the bounded mechanical publication control. Neither this fixed author nor the admitting review double qualifies live semantic quality. Failed pre-confirm output is retained under inline-production-full-baseline/; no native confirmation ran.

- Nonempty Package Setup Prerequisite (2026-09-08): The real proposal compiler rejects the scratch HIIT seed before staging because it has no active immutable baseline. This is not a confirmation failure: no native child starts and no transaction is published. Preserve inline-production-full/preconfirm-output.json. Establish the existing published-baseline prerequisite explicitly, then use real pre-confirm compilation with only a deterministic author fixture; do not manufacture a sealed JSON payload or count the prior no-op fixture as creation proof.

- No-Op Fixture Proof Rejected (2026-09-08): Native CONFIRM and retry reach the real commit kernel, return CLOSED and identical callback output with zero inference requests; 22 publication/journal files remain byte-identical on retry. However the reused unit transaction helper explicitly seals a no-op write set over an already published six-file baseline, and completion reports zero sealed writes. This does not prove a newly created governance package. Preserve the result as no-op/idempotency evidence only; require a nonempty pre-confirm write set and independent staged-byte readback before accepting native publication proof. Socket/process creation is denied inside the Python callback, and browser auto-open is disabled. Receipt: /private/tmp/odylith-native-confirmation-control.xvEV2h/results-production-publish.json.

- Stop-Protocol Alternative Rejected For Visibility (2026-09-08): The same production unavailable-hash callback, transformed only in the isolated harness to continue:false plus stopReason, invokes the native hook and exits with zero response requests. Text mode still shows only UserPromptSubmit Stopped. This alternative does not improve receipt visibility over decision:block and is not adopted. Both native termination controls remain negative transaction cases, not publication or installed-host qualification. Receipt: /private/tmp/odylith-native-confirmation-control.xvEV2h/results-production-stop-text.json. The next bounded proof exercises real sealed publication and idempotent retry without confusing fixture semantics with model quality.

- App-Server Activation Control (2026-09-08): Correct dotted override keys allow initialization and hooks/list to return the sole expected inline UserPromptSubmit hook, but its trustStatus is untrusted. The top-level one-off bypass flag does not make that hook execute in this app-server invocation: no witness or hook-completed event appears and the turn reaches the local detector. Do not interpret this as a blocking-payload failure. No external inference or transaction publication occurs, and the ephemeral process exits normally. Evidence: /private/tmp/odylith-native-confirmation-control.xvEV2h/app-server-production-unquoted/result.json. Continue only through a documented per-session trust interface; do not edit user trust records or infer eligibility from mere discovery.

- Resume Preflight Limits (2026-09-08): The prior guidance validation process is absent. Fresh validation exits 2 because the root AGENTS.md uses semantically equivalent wording instead of the validator's exact show-that-Markdown phrase; both file and token check are unchanged at HEAD. This is an existing text-contract mismatch, not host protocol regression or passing behavior proof. The first isolated app-server control exits before initialization with invalid transport for a quoted node_repl key; no hook, turn or provider request occurs. Correct the harness dotted-key encoding and retain its failed receipt before drawing any protocol conclusion. Evidence: /private/tmp/odylith-native-confirmation-control.xvEV2h/app-server-production/.

- Native Activation And Visible Outcome Limits (2026-09-08): Adding real Git project roots does not activate either flat or wrapped file-hook controls under the isolated CLI configuration; both still reach the loopback response detector without a hook witness. This does not identify the discovery cause or justify changing the shipped file format. Production-hook text mode does invoke the callback and blocks inference, but shows only the generic UserPromptSubmit Blocked status, not the complete STALE_TRANSACTION outcome. The blocking payload is therefore an unqualified candidate, not finished completion UX. Inspect the native application protocol before accepting it; do not restore model continuation to narrate deterministic completion. Evidence: results-git.json and results-production-text.json under /private/tmp/odylith-native-confirmation-control.xvEV2h/.

- Shared Decision Transport Repair (2026-09-08): Eleven consumed-decision controls fail before the payload repair while the valid EDIT continuation control already passes. The shared owner now returns the native blocking decision and complete visible reason for consumed commands, rather than sending an instruction to a later model. Hash routing, immutable publication and EDIT-with-evidence behavior are unchanged. The focused host/command/routing/journal selection passes 52 tests in 7.40 seconds. A real Codex UserPromptSubmit process invokes the production Python hook for an unavailable exact hash and terminates with zero inference requests and zero tokens; its inline registration is a transport witness, not installed activation. Native file discovery and completion visibility remain unresolved. Receipts: protocol-before.xml, protocol-after.xml and results-production.json under /private/tmp/odylith-native-confirmation-control.xvEV2h/.

- Native Prompt Termination Comparison (2026-09-08): Explicit inline hook registration establishes real UserPromptSubmit invocation for both controls in Codex CLI 0.153.1. Context-only output reaches one local /v1/responses request; decision:block exits zero with zero response requests and zero reported input/output tokens. Both make a model-catalog GET before the hook, not an inference call. This proves a narrow host-protocol distinction, not installed hook discovery, real package publication, Claude parity or visible completion: the Codex JSON event stream omits the supplied completion text. Use the existing shared host payload owner to terminate consumed decisions rather than instruct a later model not to reason. Allow only EDIT with supplied new evidence to continue into pre-confirm compilation. Keep discovery and user-visible receipt proof open. Evidence: /private/tmp/odylith-native-confirmation-control.xvEV2h/results-inline.json. Official contracts: https://learn.chatgpt.com/docs/hooks and https://code.claude.com/docs/en/hooks#userpromptsubmit-decision-control.

- Native Transport Control Preflight (2026-09-08): Four isolated Codex CLI 0.153.1 controls compare flat/context, wrapped/context, wrapped/stop and wrapped/block hook files against a loopback-only provider detector. No hook-call witness appears in any cell; each reaches one local response request and exits at the detector. These are unqualified hook-discovery controls, not evidence that either stop protocol fails or that Odylith published an unsupported transaction. No model inference or governed publication occurs. Preserve /private/tmp/odylith-native-confirmation-control.xvEV2h/results.json and per-cell output. Establish that the intended hook actually runs before comparing termination behavior. Official hook documentation distinguishes additionalContext from blocking UserPromptSubmit; the existing product callback emits context only. Do not infer native parity from Python callback equality.

- Host Eligibility And Native Proof Gap (2026-09-08): Current source passes 17 focused host tests in 5.24 seconds, proving the two Python hook main functions use the shared hash-bound callback before ordinary routing. Those tests stub commit and do not establish native Codex/Claude hook activation or absence of a later semantic model turn. A separate two-probe no-model counterexample shows public proposal rendering unconditionally offers CONFIRM and a runnable create command even when eligibility is false; confirmation_supported has no production caller, and unknown-host decisions return None rather than an explicit read-only outcome. This is an offer-boundary omission, not proof of a successful unsupported-host write or hash-integrity bypass. Preserve authorized manual CLI use and the product-neutral kernel; establish explicit adapter capability at the host-facing offer boundary, not a model-name or forgeable environment allowlist. Evidence: /private/tmp/odylith-host-confirmation-proof.czLkUO/review.md, focused.xml and offer-boundary.xml. The native-host release gate remains open.

- Status: Open

- Created: 2026-08-02

- Severity: P0

- Reproducibility: Always

- Type: DataLoss

- Description: Greenfield host confirmation resolved bare CONFIRM through a mutable singleton transaction path. A second proposal could replace the staged bytes between review and decision, causing the host callback to commit a different hash than the package the operator saw.

- Impact: An operator could approve package A while Odylith committed package B; product-domain governance truth and release delivery would no longer match the reviewed decision.

- Components Affected: domain-intelligence

- Environment(s): Codex and Claude pre-model Greenfield confirmation adapters in v0.1.15 development

- Detected By: Adversarial transaction-boundary source review and concurrent A/B schedule analysis

- Failure Signature: propose(A); propose(B) overwrites singleton; bare CONFIRM rereads B receipt and commits B

- Trigger Path: odylith greenfield propose followed by a second proposal before bare host CONFIRM

- Ownership: Domain Intelligence Greenfield pending transaction and host confirmation boundary

- Timeline: Captured 2026-08-02 through `odylith bug capture`.

- Blast Radius: All supported Codex and Claude Greenfield confirmations using the singleton pending alias; release delivery blocked

- SLO/SLA Impact: P0 delivery and release-readiness blocker for reviewed-byte confirmation integrity

- Data Risk: High product-domain data integrity risk: coordinated Radar, Registry, Atlas, and Compass truth could differ from the user-reviewed package

- Security/Compliance: Security integrity and non-repudiation boundary violated. Compliance and policy approval evidence becomes unreliable. Privacy, accessibility, and safety scope are not directly changed, but their reviewed constraints could be replaced by different pending bytes.

- Invariant Violated: CONFIRM must commit the exact immutable transaction hash shown to the operator

- Root Cause: greenfield_proposals_cli._compile_prompt_evidence_transaction always wrote product-create-transaction.v1.json at one fixed runtime path, while greenfield_host_confirmation.maybe_handle_greenfield_decision accepted bare CONFIRM and reread the adjacent receipt from that fixed path instead of resolving the hash shown in chat.

- Solution: Store pending packages under pending/<transaction-hash>, render hash-bearing CONFIRM/EDIT/REJECT commands, resolve only that exact directory, and serialize CONFIRM with REJECT through one repository lock.

- Rollback/Forward Fix: Forward fix only; reject the retired bare-command and singleton path contract.

- Verification: Hash-bound Codex/Claude parity tests, A/B retarget regression, immutable pending-store tests, shared-lock contention tests, and real propose/create integration.

- Prevention: Require every approval command to carry immutable package identity and adversarially test concurrent replacement schedules before release.

- Agent Guardrails: Never infer confirmed bytes from a mutable current pointer; reviewers must trace the user-visible identifier to the exact read path.

- Preflight Checks: No singleton pending path; exact hash in all three commands; compiler receipt and transaction-address binding pass.

- Regression Tests Added: test_hash_bound_confirm_cannot_be_retargeted_by_a_newer_pending_proposal; pending store and lock suites

- Version/Build: 0.1.15 development

- Code References: - src/odylith/runtime/domain_intelligence/greenfield_pending_transaction_store.py
- src/odylith/runtime/surfaces/greenfield_host_confirmation.py

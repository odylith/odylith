#!/usr/bin/env bash
set -euo pipefail
source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/_odylith.sh"

release_version_state_cli show --session-file "$odylith_release_session_file" --remote origin --start-at 1

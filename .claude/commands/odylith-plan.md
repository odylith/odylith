Build a bounded Odylith plan before implementation.

1. Ground the slice with `/odylith-start` or `/odylith-context` first.
2. Identify the active workstream, component, or governed source-of-truth record.
3. Produce a flat execution plan with explicit validation and closeout obligations.
4. Keep the plan bounded to the current slice instead of widening into unrelated repo cleanup.

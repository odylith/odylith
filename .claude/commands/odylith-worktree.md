Create an isolated Odylith worktree for a bounded slice.

1. Use the required branch shape `<year>/freedom/<tag>`.
2. Create the worktree under `.claude/worktrees/<tag>/`.
3. Ground the new worktree before broad repo search so the delegated slice starts with the right Odylith context.
